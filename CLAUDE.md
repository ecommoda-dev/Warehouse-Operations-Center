<div dir="rtl" style="text-align: right;">

# مركز عمليات المخزن — Warehouse Operations Center (`Warehouse-Operations-Center`)

![version](https://img.shields.io/badge/version-v1.19.0-blue)

**بتعمل إيه:** هب واحد لمحطة المخزن. الموظف بيدخل **مرة واحدة**، وبعدين
بيتنقّل بين الطباعة والتغليف وحذف المنتج ورحلة الأوردر من غير ما يدخل تاني
ومن غير ما يفتح تبويبات متفرقة.
**مين بيستخدمها:** المخزن
**الإصدار:** `v1.19.0` — **واحد للهب كله** (`TOOL_VERSION` في `shared/shell.js`)

> 🔴 **الهب واجهة بحتة.** مفيش Worker جديد، مفيش `wrangler.toml`، مفيش
> `index.js`، مفيش ربط Cloudflare Builds ومفيش Promote **من الريبو ده**.
> بينادي **أربع** Workers عايشين في ريبوهاتهم.

> ⚠️ **«الأربعة» من v1.6.0** — `order-sku-barcode-printer-worker` انضم
> (صفحة `sku-barcode.html`). الـ Worker ده في ريبو
> `Order-SKU-Barcode-Printer`، والـ Promote بتاعه **من هناك** — القاعدة فوق
> ما اتكسرتش.

> 🔴 **الأدوات القديمة التلاتة لسه منشورة وشغّالة** كنقطة رجوع طول التجربة
> الحية. تحويلها لصفحات تحويل = قرار منفصل بعد نجاح التجربة.

## الروابط

```
الواجهة : https://ecommoda-dev.github.io/Warehouse-Operations-Center/
tool في D1 : warehouse_ops_center      ← login · logout بس
مجموعة السر : warehouse_ops            ← ٤ Workers + الهب (خمس مستهلكين)
مفتاح localStorage : warehouse_ops_worker_secret   ← Standards #39
```

## الصفحات والـ Workers

| الصفحة | الأداة | الـ Worker | الحد الأدنى | Tier |
|---|---|---|---|---|
| `index.html` | الدخول + الشاشة الرئيسية | التغليف (للدخول) + التلاتة (للأعداد) | — | L (1400) |
| `print.html` | قسم الطباعة — فواتير **وبوالص بوسطة** | `order-printer-worker` | **`2.5.0`** | L (1400) |
| `pack.html` | قسم التغليف | `orders-packing-checker-worker` | **`2.5.0`** | L (1400) |
| `remove.html` | حذف منتج من الأوردر | `order-item-remover-worker` | **`1.4.0`** | M (1200) |
| `journey.html` | رحلة الأوردر | التلاتة (قراءة سجل) | — | M (1200) |
| `stats.html` | إحصائيات المخزن | التلاتة (قراءة سجل) | — | L (1400) |
| `sku-barcode.html` | باركود SKU — **دفعة أوردرات + سجل** | `order-sku-barcode-printer-worker` + التغليف (تراكينج · موظفين) | **`1.3.0`** · pack `2.5.0` | M (1200) |

> ⚠️ **أربع `min` مستقلة تمامًا** — مالهمش أي علاقة ببعض ولا بـ
> `TOOL_VERSION`. `min` مايترفعش إلا لما الهب **يعتمد فعلاً** على حاجة جديدة
> في الـ Worker ده (Standards #29). ترفيعه بلا سبب = تحذير كاذب على أي
> rollback مشروع.

> ⚠️ **`printer.min = '2.5.0'` مشروع — واترفع على مرتين:**
> **①** في v1.7.0 بقى بينادي `POST /lookup` (Worker v2.4.0) — المسار اللي
> بيجيب أوردر بعينه مهما كانت حالته، وهو **الشرط الوحيد** لإعادة طباعة
> فاتورة أوردر خرج من الطابور. Worker أقدم مافيهوش المسار فبيرجّع 404،
> والكارت بيقول بالنص «محتاجة Worker v2.4.0» بدل «الأوردر مش موجود».
> **②** في v1.9.0 بقى معتمد على `?action=bosta_lookup` و`?action=bosta_awb`
> و`allZones` في `/orders` و`doc: 'AWB'` في `/track` (Worker v2.5.0) —
> **قناة بوسطة مش موجودة أصلاً من غيرهم**.
> (وقبلهم في v1.5.0 كان الاعتماد على `printingTimeS1`/`packingTimeS1`
> في رد `/orders` — Worker v2.3.0.)

> ⚠️ **`remover.min = '1.4.0'` مشروع (اترفع في v1.8.0):** `remove.html` بقت
> بتعتمد على حارس **§PREFLIGHT** في الـ Worker — الوعد اللي الصفحة بتقوله
> للموظف دلوقتي هو «قيمة مرفوضة = العملية بتتلغي بالكامل والأوردر ما بيتلمسش».
> Worker أقدم مافيهوش الحارس، فبيحذف البند الأول ويفشل في كتابة الحالة/السبب
> بعدين — بالظبط الباج اللي النسخة دي اتكتبت عشانه، وبند متحذف **مالوش رجعة**.
> ودي **مش** حالة تدهور آمن، فالحارس هنا مش رفاهية.

> ⚠️ **`barcode.min = '1.3.0'` مشروع — واترفع على أربع مرات:**
> **①** في v1.10.0 بقى `1.1.0` — **أول نسخة بتقبل `get_order?id=`**. باركود
> الأوردر المطبوع بيشفّر الـ **ID الرقمي** (١٠ خانات فأكتر) مش اسم الأوردر
> (٥ خانات)، فماسح باركود شوبيفاي بيبعت `id=` — وعلى Worker `1.0.0` الرد
> بيبقى `رقم الأوردر مطلوب` (400) على **كل مسح**.
> **②** في v1.15.0 بقى `1.2.0` — **أول نسخة فيها سجل العمليات**
> (`log_print` · `get_logs` · `get_logs_count` · `get_logs_export`)
> و`[[d1_databases]]`. `sku-barcode.html` بقى فيها **تاب سجل** بينادي التلات
> مسارات دي، وكل ضغطة طباعة بتنادي `log_print`. على Worker `1.1.0` التاب
> بيرجّع «action غير معروف» على كل تحديث، والطباعة بتشتغل **من غير أي أثر
> في D1**. الصفحة معتمدة على المسارات دي فعلاً (Standards #29).
> ⚠️ **التدهور هنا جزئي ومُعلَن:** مربع «طباعة بالـ SKU» بيشتغل على `1.1.0`
> عادي (`search_sku` موجود من `1.0.0`)، وفشل `log_print` **مايمنعش الطباعة**
> (توست أصفر والورق بيطلع) — وحارس النسخة بيقول السبب بالاسم.
> **③** في v1.16.0 بقى `1.2.1` — **أول نسخة `search_sku` فيها بتفهم الـ SKU
> الكامل**. الاستعلام القديم كان بيبعت المدخل لشوبيفاي زي ما هو، وبحث
> شوبيفاي بيقسّم `SD1 / Light grey / 45` لكلمات منفصلة — فبيرجّع **صفر
> نتايج بلا أي خطأ** على صنف موجود قدام الموظف في الجدول. و`sku-barcode.html`
> بقى فيها **قايمة اقتراحات بتفتح وانت بتكتب** (§SKU-AC تحت) — معتمدة على
> السلوك ده بالحرف، فالترفيع مش رفاهية.
> ⚠️ **والتدهور هنا صامت لو الحارس ما اترفعش:** القايمة بتفضل فاضية
> والرسالة بتقول «مفيش أي صنف بالـ SKU ده على شوبيفاي» — جملة **صح شكلاً
> وغلط فعلاً**. ده بالظبط نوع الفشل اللي حارس النسخة اتكتب عشانه.

> **④** في v1.17.0 بقى `1.3.0` — **أول نسخة بترجّع `available` و`productId`**.
> الصفحة بقت بتعرض **الكمية المتاحة على شوبيفاي** لأصناف الـ SKU (وعدد
> الطباعة بيتبعها)، وبتخلّي **الـ SKU لينك لصفحة المتغيّر في الأدمن** —
> والرابط **مركّب من `productId` و`variantId` مع بعض**. على `1.2.1` الخانة
> بتقول رقم **مش** الكمية والـ SKU بيفضل نص عادي: **رقم غلط شكله سليم**.
> ⚠️ **و`available` تحت صلاحية `read_inventory`** في الـ Custom App —
> **مش حاجزة**: الـ Worker بيرجع لاستعلام لين من غيرها والبحث بيفضل شغّال،
> والخانة بتقول `—` (مش صفر — «مش معروف» ≠ «مفيش في المخزن»). بند
> `صلاحية read_inventory` في `?action=diag` بيقول الغياب بالاسم.

> ✅ و`search_sku` **رجع ليه مستهلك في v1.15.0** — البند اللي كان مفتوح
> («يتشال ولا يفضل؟») اتقفل بالإبقاء، والشيل بقى تغيير كاسر مش تنضيف.

> ⚠️ **`pack.min = '2.5.0'` مشروع:** الهب بيبعت `appId` في `verify_employee`
> و`log_logout`، والباراميتر ده اتضاف في Worker التغليف v2.5.0. نسخة أقدم
> **مش هترجّع خطأ** — هتسجّل الدخول تحت `pack_checker` **في صمت**، وده
> بالظبط نوع الفشل اللي الحارس اتكتب عشانه.

> 🔴 **`sku-barcode.html` بقت بتنادي Worker التغليف كمان من v1.10.0** —
> في حالتين بس (التانية اتضافت في v1.15.0):
> **①** تحويل تراكينج بوسطة لرقم أوردر (`get_order?tracking=`).
> **②** `get_employees` لتعبئة فلتر الموظف في **تاب السجل** — Worker
> الباركود **مالوش §SHARED ومالوش نقطة دخول بقرار** (الانحراف ٢ في
> `Order-SKU-Barcode-Printer/CLAUDE.md`)، فنسخ الكتلة دي هناك كان هيدّي
> نقطة دخول تانية مالهاش لازمة. والاتنين **قراءة بحتة**.
> القرار ده **مقصود ومكتوب**: §BOSTA موجود في
> ريبوهين بالفعل (خطر مفتوح تحت)، ونسخة تالتة في Worker الباركود كانت
> هتخلّيهم **تلاتة** يفترقوا مع أول إصلاح في شكل رد بوسطة. النداء **قراءة
> بحتة**، والحاجة الوحيدة المستخدمة من رده هي **اسم الأوردر**، وبعدها
> الدفعة بتكمّل على Worker الباركود بنفس المسار بالحرف.
> ⚠️ عشان كده `PAGE_WORKERS` في `sku-barcode.html` بقت `['barcode', 'pack']`
> — حارس النسخة لازم يسمّي الاتنين، وإلا فشل مربع بوسطة على Worker تغليف
> قديم بيبان كأنه مشكلة في أداة الباركود.

## 🔴 الاستثناءان المعتمدان من `ecommoda-html-builder`

الهب بيكسر بندين من Step 2 (**Non-Negotiable UI Rules**) **بقرار صريح**.
البندان لسه ساريان بالكامل على **الأدوات المستقلة** — الاستثناء لأداة **هب** بس.

### الاستثناء ١ — بند ١: «ONE HTML file per tool»

الهب **متعدد الصفحات + ملفين مشتركين**. الأرقام اللي بنى عليها القرار:

| | ملف واحد | متعدد الصفحات |
|---|---|---|
| أكبر ملف عند ٣ أدوات | ~٤٢٠ KB | **٢٢٠ KB** |
| أكبر ملف عند ٨ أدوات | **~١.٢ MB** | **٢٢٠ KB** (ثابت) |
| تعارضات CSS تتحل بإيدك | ٤٦ | **صفر** (قاعدة الأسبقية تحت) |
| باج في التغليف يوقّف الطباعة | ✅ | ❌ |
| فوكس السكانر | يتدار مركزيًا (خطر) | زي النهاردة بالظبط |

### الاستثناء ٢ — بند ٥: «`currentEmployee` in JS memory only»

الهوية بتعيش في **`sessionStorage`** عشان تعيش عبر التنقل بين الصفحات.

| | `localStorage` (الممنوع) | `sessionStorage` (الهب) | الذاكرة (الأدوات المستقلة) |
|---|---|---|---|
| بيعيش بعد قفل التاب | ✅ **خطر** | ❌ | ❌ |
| مشترك بين تابين | ✅ **خطر** | ❌ **لكل تاب لوحده** | ❌ |
| بيعيش عبر التنقل | ✅ | ✅ **المطلوب** | ❌ |
| بيدي صلاحية جديدة | ❌ | ❌ | ❌ |

> ⚠️ **الحقيقة اللي لازم تتقال صريحة:** `employee` بيتبعت من العميل في كل
> كتابة **النهاردة كمان** — الـ Worker مابيتحققش من هوية الموظف، بيتحقق من
> السر بس. `sessionStorage` **مابيضيفش** أي ضعف؛ هو بيوصل الهوية بين الصفحات
> وبس. المحاسبة على الموظف **شرف مش إثبات** — قبل الهب وبعده. لو اتطلب تتحوّل
> لإثبات، الحل session token موقّع من الـ Worker، وده بند منفصل تمامًا مكانه
> `ecommoda-worker-builder`.

**فرق سلوكي واحد لازم يتقال:** في الأدوات المستقلة أي **ريفريش** بيطلّع
الموظف. في الهب الريفريش **بيحافظ على الجلسة** (وقفل التاب بيطلّعه). ده
**تحسين مقصود** — `pack_checker` عنده ٣٠٠ صف `login` في ٤ شهور، جزء كبير منهم
ريفريش مش دخول جديد.

## 🔴 قاعدة الأسبقية — أهم قاعدة في الريبو ده

```html
<!-- في كل صفحة، بالترتيب ده بالظبط -->
<link rel="stylesheet" href="shared/shell.css">
<style>
  /* CSS الصفحة — بيغلب shell.css عند أي تعارض */
</style>
```

**`shell.css` فيه الـ chrome بس. أي CSS خاص بأداة بيفضل في صفحته.**

ليه ده بيلغي شغل حل الـ ٤٦ تعارض: الصفحة بتيجي **بعد** الـ shell، فقاعدتها
بتغلب تلقائيًا. يعني `.act-btn` و`.count-badge` و`.state-box` و`.ms-*`
و`.tbl-*` و`.range-preset-*` و`.rdy-*` **كلها تفضل في صفحتها زي ما هي** —
صفر مخاطرة انحدار بصري.

| ✅ يدخل `shell.css` | ❌ مايدخلش (يفضل في الصفحة) |
|---|---|
| كتلة التوكنز كلها | أي كلاس خاص بجدول أو فلتر |
| reset + `body` + `.container` | `.data-table` · `.tbl-*` · `.col-*` |
| `.app-header` · `.app-icon` · `.app-title` · `.app-header-btns` · `.hbtn` | `.ms-*` · `.flt-*` · `.chips-*` · `.unified-section` |
| `.main-tabs-bar` · `.main-tab-btn` · `.card` | `.range-preset-*` · `.act-btn` · `.state-box` |
| `.eco-overlay` · `.eco-modal*` · `.modal-close-x` | أي كلاس اسمه فيه `rdy-` |
| `.settings-*` · `.about-sec*` · `.cl-*` · `.diag-*` | `.sticky-header-wrap` · `.active-tab-chip` (الطابعة) |
| `.toast-container` · `.toast` + أنواعه | CSS الفاتورة جوّه الـ iframe |
| `.btn-primary` · `.btn-ghost` · `.btn-green` · `.btn-red` · `.btn-outline` | |
| `.order-link` · `.order-num` · `.order-name` · `.no-link` | |
| `--font-label` (للطباعة بس) | `.bc-*` · `.entry-*` · `.scan-*` · **`.sku-ac*`** (باركود SKU) · `.print-label` · `.lbl-sku-text` · `.log-*` · `.data-table` · `.pagination-row` |
| `.woc-home-*` · `.woc-row*` · `.woc-tool*` (ومعاه **`.tool-danger`**) · `.woc-card-ico/-name/-desc` · `.woc-num` · `.woc-chip*` (ومعاهم `.qc-bosta`/**`.qc-showroom`**/`.qc-courier`) · `.woc-fresh*` · `.woc-ago*` · `.woc-stamp-chip` · `.woc-rbtn*` · `.woc-fail` · `.woc-back-bar` | |

> ⚠️ **`.lbl-order-text` اتشال من القايمة في v1.15.0** — سطر رقم الأوردر
> نفسه اتشال من الليبل (§LABEL-2LINE تحت)، فالكلاس مابقاش موجود أصلاً.

> 🔴 **كتلة التوكنز في `shared/shell.css` بس.** أي كتلة توكنز في صفحة = التوكنز
> اتفرّقت. **الاستثناء الوحيد المسموح:** `remove.html` و`journey.html`
> و`sku-barcode.html` بيعرّفوا `--container-max: 1200px` لوحدها (Tier M) وخلاص.

> ⚠️ **`--font-label` في الـ shell — وهو استثناء موثّق من Standards #11**
> (خط واحد `Noto Sans Arabic` لكل الأدوات). الاستثناء متحدّد بـ **الليبل
> المطبوع** في `sku-barcode.html` بس. **ممنوع أي عنصر على الشاشة
> يستخدمه** — نفس عيلة استثناء ألوان الفاتورة جوّه الـ iframe.
> 🔴 **قيمته اتغيّرت في v1.15.0** من `Georgia, 'Times New Roman', serif`
> لستاك grotesque محايد (`Arial, 'Helvetica Neue', Helvetica,
> 'Liberation Sans', Arimo`). المبرّر القديم كان مطابقة تيمبلت «Retail
> Force Barcode» الخارجي — **والمطابقة دي بطلت تبقى الهدف** لما الليبل بقى
> سطرين بدل تلاتة (§LABEL-2LINE). السبب الكامل مكتوب **جنب التوكن نفسه في
> `shared/shell.css`** — مش هنا، عشان مايبقاش فيه مصدرين.
> ⛔ **وممنوع يبقى خط من الشبكة** — CDN محجوب = خط احتياطي بعرض حروف مختلف
> = قياس `bcFitLabelText` اتحسب على خط ما اتحمّلش = نص بيخرج بره الليبل
> **من غير أي خطأ**.

**التوكنز المتعارضة — القرار المتنفَّذ:**

| التوكن | الطابعة | التغليف | الحذف | **المعتمد** |
|---|---|---|---|---|
| `--text-muted` | `#9ca3af` | `#6b7280` | `#6b7280` | **`#6b7280`** |
| `--amber-border` | `#fcd34d` | `#fde68a` | — | **`#fde68a`** |
| `--container-max` | `1400px` | `1400px` | `1200px` | **`1400px`** في الـ shell · Tier M بينزّلها |
| `--shadow` | مختلف بالمسافات | — | — | **نسخة التغليف** |

> ⚠️ **`--print-ink` و`--print-paper`** في الـ shell، بس **HTML الفاتورة جوّه
> الـ iframe بيفضل بألوانه الحرفية** — استثناء موثّق من Standards #35، لأن
> مستند الـ iframe مالوش وصول للتوكنز. **متحاولش تصلّحه.**

> ⚠️ **`--font-mono` لازم يفضل معرّف** بنفس قيمة `--font-body` — كلاسات
> (`.order-link` · `.col-num` · `.settings-input`) بتستخدمه (Standards #16).

## 🔴 `shared/shell.js` — نسخة واحدة مضمّنة

**ممنوع الملف ده يتنسخ في أي صفحة.** نسخة تانية معناها إن الصفحتين هيفترقوا
مع أول تعديل، وده بالظبط الوضع اللي أنتج باج R1 بين `Ready-to-Pack`
و`Orders-Packing-Checker` (منطق واحد في ملفين، اتصلح في واحد وفضل مكسور في
التاني **لشهور**).

| القسم | المحتوى |
|---|---|
| §CONFIG | `WOC_WORKERS` · `TOOL_VERSION` · `LS_SECRET` · `WOC_APP_ID` · `getSecret`/`setSecret`/`isConfigured` |
| §SESSION | `getSession` · `setSession` · `clearSession` · `requireSession` · `cacheGet` · `cacheSet` |
| §API | `wocApi(worker)` — **مصنع** بدل دوال عامة |
| §HELPERS | `esc` · `toCairo` · `cairoDayStr` · `cairoDayIndex` · `formatDate*` · `arMin`/`arHour`/`arDay` · `sinceText` · **`agoInfo`** · **`wocDayDiff`/`wocDayLevel`/`wocOrderAge`** · `orderLink` · `shopifyOrderUrl` · **`shopifyVariantUrl`** · `cmpVersion` · `playBeep` |
| §BOSTA-GATE | `BOSTA_UPLOADED_TAG` · `wocBostaUploaded` · `wocChannelOf` · `wocChannelGate` |
| §COURIER-GROUP | **`wocCourierGroup`** · **`WOC_COURIER_GROUPS`** · **`wocCourierCounts`** — بوسطة/**شو روم**/مناديب (تلاتة من v1.14.0). `index.html` و`pack.html` بيعدّوا منهم الاتنين |
| §UI | `showToast` · الإعدادات · `wocRunDiag` · `renderVersionUI` · `checkWorkerVersion` · `showWorkerStale` · About/Changelog · `doLogout` |
| §HEADER | `wocHeader(opts)` · `wocSharedModals()` |

### 🔴 القايمة السودا — الدوال دي ممنوع تدخل الـ shell

```
switchMainTab · finishLogin · runDiag · renderLogTable · fetchLog
renderPagination · goPage · lookupOrder · currentOrder · applySort
sortConfig · msState · msToggle · anyFilterActive · clearAllFilters
```

من ٦١ اسم مشترك بين الطابعة والتغليف، ~١٥ منهم **منطقهم مختلف فعلاً** مش مجرد
تنسيق. **حطّهم في الـ shell = تكسر صفحة أو اتنين بصمت.** كل واحد فيهم يفضل في
صفحته.

> ⚠️ نفس المبدأ خلّى `prtOrderLink`/`prtShopifyOrderUrl` يفضلوا في
> `print.html` باسم خاص بيهم: نسخة الطابعة بتضيف `#` وبتفكّ الـ `gid`
> وبتروّت على `/admin/orders/` — **مختلفة فعلاً** عن نسخة الـ shell،
> فبتفضل جنبها بدل ما تدهسها.

> ⚠️ **`renderAboutWorkers()` مكرّرة في الست صفحات عن قصد.** `WOC_WORKERS`
> و`cmpVersion` في الـ shell، بس **فحص Step 9 بيطلب وجود الاسمين في كل صفحة**،
> والقايمة نفسها (`PAGE_WORKERS`) بتفرق من صفحة للتانية.

## عقد الجلسة — `sessionStorage`

```js
const WOC_SESSION_KEY  = 'woc_session';       // الهوية
const WOC_CACHE_PRINT  = 'woc_queue_print';   // كاش طابور الطباعة
const WOC_CACHE_PACK   = 'woc_queue_pack';    // كاش طابور التغليف
const WOC_CACHE_TTL_MS = 15 * 60 * 1000;      // ١٥ دقيقة
```

```js
{ v: 1, username: 'Ahmed_Ibraheem', displayName: 'Ahmed Ibraheem', loginAt: '…Z' }
```

- **`v` نسخة العقد** — لو الشكل اتغيّر، القديم بيترفض بدل ما يتقري غلط.
- **كل قراءة/كتابة جوّه `try/catch`** — المتصفح في وضع خاص أو بحظر التخزين
  بيرمي على **مجرد الوصول**، والرمي ده كان هيمنع فتح الصفحة أصلاً.
- **مفيش انتهاء صلاحية بالوقت** — الجلسة بتموت بقفل التاب أو بالخروج بس.
  أي timeout كان هيطلّع الموظف وهو بيغلّف.

### `requireSession()` — أول سطر في كل صفحة محمية

> 🔴 **بترمي عن قصد.** من غير الرمي، باقي سكربت الصفحة بيكمّل تنفيذ وبينادي
> الـ Worker وبيرسم جدول **أثناء** ما التحويل شغّال. النتيجة نداءات ضايعة
> ووميض شاشة.

> ⚠️ **`location.replace` مش `location.href`** — عشان صفحة محمية مايتسجّلش لها
> مدخل في تاريخ المتصفح. زرار الرجوع بعد الخروج مايرجعش لشاشة فاضية.

### كاش الطوابير

```js
{ at: '2026-09-05T07:12:44.101Z', data: <رد الـ endpoint كامل> }
```

- بيتكتب بعد **كل جلب ناجح** من أي صفحة.
- `print.html` و`pack.html` بيقروا كاشهم عند الفتح فالشاشة **مش بتفضل فاضية**،
  والرسم من الكاش **مختوم بوقته**.
- أقدم من ١٥ دقيقة: ترسم من الكاش فورًا **و**تجيب تحديث. الشاشة الفاضية أسوأ
  من شاشة قديمة **مختومة بوقتها**.
- `QuotaExceededError` بيتبلع بصمت — الكاش **تحسين مش شرط**.

## الهيدر الموحّد — الترتيب

RTL، القراءة من اليمين للشمال:

```
[العنوان]        [🏠 الرئيسية]        [extras] [⚠️ نسخة قديمة] [ℹ️ عن الأداة] [vX.Y.Z 📋] [اسم ✕] [⚙️ الإعدادات]
   يمين              النص                            شمال (بترتيب القراءة RTL)
```

> ⚠️ **`⚙️ الإعدادات` آخر عنصر** في ترتيب القراءة RTL = **أقصى الشمال بصريًا**
> (Step 3 · Header Button Order). الأدوات التلاتة الحالية **مش متطابقة** في
> الترتيب ده — الهب بيوحّده.

### 🔴 انحراف مقصود — `🏠 الرئيسية` في النص (قرار أحمد 06-09-2026)

Step 3 بيقول الرئيسية **أول عنصر** في قراءة RTL (أقصى اليمين) وبنفس شكل
باقي `.hbtn`. الهب بيكسر البندين:

| | Step 3 | الهب |
|---|---|---|
| المكان | أول عنصر — أقصى اليمين | **منطقة وسطى** بين العنوان والزراير |
| الشكل | `.hbtn` عادي | **`.hbtn-home` مصمت أزرق** |

**السبب:** الموظف بيرجع من الزرار ده من **كل** أداة، وكان بنفس شكل
«الإعدادات» و«عن الأداة» بالظبط فبيتوه وسطهم.

**التنفيذ:** `.app-header.has-center` بيبقى `display:grid` بـ
`1fr auto 1fr`، و`wocHeader` بيحقن `.app-header-center` **بس لما
`home: true`**.

> ⚠️ **تحت 1100px الشبكة التلاتية بتخنق الزراير** — فيه media query بترجّع
> `display:flex`، و`justify-content:space-between` بيسيب الزرار في النص
> برضه. متشيلهاش: من غيرها الهيدر بيعمل تمرير أفقي على الشاشات الضيقة.

> ⚠️ **`🏠 الرئيسية` مايتعرضش في `index.html`** (`home: false`) — إحنا فيها
> أصلاً، والهيدر بيرجع `display:flex` عادي من غير المنطقة الوسطى.

### زرار الموظف = زرار الخروج

```
[اسم الموظف ✕]     ← مش [👤 اسم — خروج]
```

كلمة «خروج» بقت **أيقونة ✕** و`👤` اتشالت — الاسم لوحده كافي للتعريف
(قرار أحمد 06-09-2026).

> ⚠️ **`aria-label="تسجيل الخروج"` على الزرار إلزامي** — الـ `✕` جوّه
> `aria-hidden`، فمن غير الـ label الزرار بيتقري «اسم الموظف» بس عند قارئ
> الشاشة، من غير أي إشارة إنه بيخرّج.

### حارس النسخة لازم يسمّي الـ Worker

الهب بينادي تلات Workers بتلات حدود دنيا مستقلة. رسالة «الـ Worker نسخة
قديمة» **من غير اسم الأداة** بتخلّي الموظف يدوّر في التلاتة.

> ⚠️ `checkWorkerVersion` **بتقف لو مفيش سر**. من غير الحارس ده أول نداء بيقع
> في `apiRequest` اللي بيفتح شاشة الإعدادات — فتطلع فوق شاشة الدخول وتغطّي
> زرار الإعدادات اللي جوّه الكارت (Standards #4). **الباج ده حصل فعلاً واتصلح
> في التطوير.**

## الشاشة الرئيسية — القواعد

| # | القاعدة | ليه |
|---|---|---|
| ١ | 🔴 **التحديث التلقائي بحراسه التلاتة** (تحت) | القاعدة القديمة كانت «مفيش polling هنا» — **اتلغت في v1.2.0**. الحراس هم اللي بيمنعوا التكلفة، مش غياب الـ polling |
| ٢ | الأعداد بتتجاب بعد الدخول، والاتنين **بالتوازي** بـ `allSettled` | Workers مختلفين = isolates مختلفين = توازي حقيقي. `allSettled` مش `all` — فشل طابور مايخفيش التاني |
| ٣ | **العدد قبل أول جلب ناجح = `—` مش `0`** | «ما اتحدّثش» ≠ «مفيش شغل» |
| ٤ | **الفشل بيعرض حالة فشل مش صفر** | «تعذّر الاستعلام» ≠ «مفيش شغل». الصف فيه ⚠️ + «حاول تاني»، والدخول للأداة **لسه شغّال** |
| ٥ | **ختم الوقت بيتسجّل حتى لو الطابور رجع فاضي**، وفي الفشل **مايتغيّرش** | «اتحدّث ورجع صفر» معلومة. وما نقولش للموظف إن القايمة جديدة وهي مش جديدة |
| ٦ | أداة جديدة بتدخل **قسم «أدوات»** افتراضيًا — و**قسم «أخرى»** لو مرجع بيتقرا مش شغل بيتعمل (v1.14.0) | عدّاد حيّ = جلب تقيل عند كل دخول وكل تحديث تلقائي. والتفرقة بين القسمين: «أدوات» بيتنادى **وسط الشغل** والموظف مستعجل، فصف فيه أربع مربعات متشابهة بيخلّيه يقرا الأربعة كل مرة |
| ٧ | الضغط على الصف بيفتح الصفحة **حتى والجلب فشل** | الطابور مش شرط للشغل — الموظف يقدر يسكن باركود عادي |
| ٨ | 🔴 **الرقم بيعدّ اللي ينفع يتطبع دلوقتي** (v1.11.1) — والمستبعَد له مربعه | الرقم وعد بشغل موجود. أوردر ما اترفعش على بوسطة مالوش بوليصة، فعدّه في «جاهز للطباعة» بيدّي الموظف رقم مايقدرش يشتغل عليه. المربعان (`⏳ مستنية رفع على بوسطة` · `⏸️ استبدال بوسطة (مؤجّل)`) بيظهروا **بس لما يكون فيهم فعلاً** — نفس مبدأ «بلا قناة» |
| ٩ | 🔴 **التقسيمة بتوصف اللي الموظف بيتصرّف عليه** — الطباعة بالقناة، والتغليف **بالمندوب** (v1.13.0 · تلات مربعات من v1.14.0) | «عادي/استبدال-استرجاع» رقم صحيح بس الموظف مابيتصرّفش عليه: الطابور بيتغلّف بنفس الطريقة في الحالتين. اللي بيفرق فعلاً **بوسطة ولا مندوب** — ورق وملصق ومحطة تسليم مختلفين. ودي **نفس** تقسيمة مربعات `pack.html` اللي الصف بيفتح عليها |

### التخطيط — صفوف مش كروت (اتجاه B · قرار أحمد 06-09-2026)

```
الطوابير  →  صف بعرض الشاشة لكل طابور  (.woc-row)
              [الهوية] [الرقم] [التقسيمة + الفشل] [لوحة آخر تحديث + زرار]
أدوات     →  مربعان مضغوطان             (.woc-tool داخل .woc-tools-grid)
              حذف منتج (ثيم أحمر) · باركود SKU
أخرى      →  نفس الشبكة بالحرف          رحلة الأوردر · إحصائيات المخزن
```

> 🔴 **قسم «أدوات» اتقصر على أداتين وقسم «أخرى» اتضاف (قرار أحمد 08-09-2026).**
> التفرقة **مش تنظيم**: «أدوات» بيتنادى والموظف واقف على أوردر ومحتاج يتصرّف،
> و«أخرى» بيتقرا في وقت تاني خالص. صف فيه أربع مربعات متشابهة معناه إن الموظف
> المستعجل بيقرا الأربعة كل مرة عشان يلاقي واحد.
> ⚠️ **والسطر التوضيحي اتشال من مربعَي «أدوات» بس** — كان بيوصف تفاصيل داخلية
> (شرط COD · مقاس الليبل) مش بيساعد في الاختيار. مربعات «أخرى» محتفظة بسطرها
> لأن اسمها لوحده مايقولش إيه اللي جوّه.

> 🔴 **`.woc-tool.tool-danger` (ثيم أحمر) للأدوات الهدّامة بس** — `remove.html`
> النهاردة. بتحذف بند من أوردر حي و**بند متحذف مالوش رجعة**، فاللون تحذير مش
> تزويق: الموظف بيدوس على المربعات دي بسرعة ولازم يفرّق الهدّامة عن القراءة
> **قبل** ما يقرا الاسم. ⛔ ممنوع يتحط على أي مربع تاني — لون تحذير على كل
> حاجة = لون بلا معنى. والاسم بقى **«عطلان؟ احذف المنتج من الأوردر»** عشان
> يطابق كلمة الزرار في جدول التغليف (`عطلان`).

**ليه صفوف:** الصف بيوسّع الرقم والتقسيمة والختم والزرار **على سطر واحد**،
وبيسع **رسالة فشل كاملة** من غير ما يطول ويبوّظ محاذاة اللي جنبه — وده اللي
كان بيحصل في تخطيط الكروت.

> ⚠️ **طابور تالت بيدخل صف زيادة — مش تخطيط جديد.** ونفس الشيء في
> `.woc-tools-grid`: `auto-fit` معناها أداة جديدة بتدخل الصف من غير أي
> تعديل CSS (المرشّحان: صفحة «التنبيهات» في `docs/ALERTS-TOOL-PLAN.md` ·
> صفحة «إحصائيات اليوم» في `docs/STATS-PAGE-PLAN.md`).

> ⚠️ **`.woc-row-mid` هي اللي بتتمدّد** (`flex:1 1 230px`) — التقسيمة والفشل
> جوّاها. لو حد حط `flex:1` على منطقة تانية، منطقة الأكشن بتتزحلق من
> `margin-inline-start:auto` وبتقع في نص الصف.

> 🔴 **لوحة «آخر تحديث» جوّه `.woc-row-act` مع الزرار — مش في `.woc-row-mid`**
> (قرار أحمد 06-09-2026). الختم وعدّاد القِدَم والزرار **وحدة واحدة**: دول
> بيتكلموا عن نفس الحاجة (الرقم ده اتجاب إمتى وإزاي تجيبه تاني)، وتفريقهم
> بيخلّي الموظف يقرا الختم في ناحية ويدوّر على الزرار في ناحية تانية.

> ⚠️ **`.woc-fresh` نسخة طبق الأصل من `.rdy-fresh` في `pack.html`** — نفس
> الحدود والظل والحشو والمقاسات. **الشكلين لازم يفضلوا متطابقين**: لوحة
> بشكلين مختلفين لنفس المعلومة = الموظف بيتعلّمها مرتين. أي تعديل على
> واحدة يتعمل في التانية في نفس التمريرة (درس R1).

> ⚠️ **`align-items:stretch` على `.woc-row-act`** هو اللي بيخلّي الزرار
> بنفس ارتفاع اللوحة بالظبط — زي `.rdy-actions`. شيله والزرار هيتوسّط
> جنب اللوحة بارتفاع أقل.

### 🔴 التحديث التلقائي — ١٥ دقيقة (قرار أحمد 06-09-2026)

```js
const WOC_AUTO_REFRESH_MS = 15 * 60 * 1000;   // نفس فترة `pack.html`
const WOC_TICK_MS         = 30 * 1000;        // نبضة عرض — صفر شبكة
```

**القاعدة القديمة «🔴 مفيش polling على الشاشة الرئيسية» اتلغت.** السبب
اللي خلّاها تتلغي: `pack.html` بتعمل polling كل ١٥ دقيقة على **نفس**
`get_ready_orders` من v2.4.0 — يعني المبدأ اتكسر من ٤ أيام، والرئيسية
كانت الوحيدة الفاضلة عليه.

⚠️ **الحراس التلاتة مش تفاصيل تنفيذ — هم القرار نفسه.** من غيرهم ده بيبقى
polling أعمى على أتقل endpoint في الستاك × عدد الأجهزة × طول اليوم، وده
بالظبط اللي القاعدة القديمة اتكتبت عشانه:

| # | الحارس | المكافئ في `pack.html` |
|---|---|---|
| ① | التسليح بعد **كل جلب ناجح** (`armAuto` جوّه `loadQueue`) — «١٥ دقيقة من آخر تحديث فعلي» مش من الدخول | `rdyArmAutoRefresh()` |
| ② | `document.hidden` = **مفيش استعلام**. بيتعلّم `dirty` والجلب بيحصل على `visibilitychange` | `rdyQueueVisible()` |
| ③ | النبضة (٣٠ ثانية) بتحدّث «منذ كام» **بلا أي نداء شبكة** | `rdyTick()` |

> ⚠️ **تايمر لكل طابور لوحده** (`homeQ[key].timer`) مش تايمر واحد للاتنين —
> Workers مستقلين وواحد ممكن يقع والتاني ينجح (قاعدة ٤). تايمر موحّد كان
> هيربط تسليح الناجح بفشل التاني.

> ⚠️ **التسليح بيحصل من الدخول كمان** مش من أول نجاح بس — لو أول جلب فشل،
> تفضل فيه محاولة تلقائية بدل ما الصف يقف على ⚠️ للأبد.

> ⚠️ **`loadQueue` هي المسار الوحيد للجلب** — الضغطة اليدوية والنبضة
> التلقائية الاتنين بيعدّوا منها. نسخة تانية للتلقائي هتفترق مع أول
> تعديل (درس R1).

> ⚠️ **`dirty` مابيتصفّرش غير بعد جلب ناجح** — لو الجلب فشل، أول رجوع
> تاني للتاب بيحاول. و`loading` بيمنع نداءين متوازيين (ضغطة + نبضة).

> ⚠️ **زرار «تحديث» جوّه كل صف لوحده — ممنوع يتوحّد.** زرار واحد للاتنين
> بيخبّي مين اللي فشل، وده بالظبط اللي قاعدة ٤ اتكتبت ضده.

> ⚠️ **الفشل بعد نجاح سابق: الرقم القديم بيفضل معروض** (مش «تعذّر») —
> والعدّاد الأحمر فوقه هو اللي بيقول إنه قديم. «تعذّر» بتتعرض **بس** لو
> مافيش أي جلب ناجح قبل كده (`homeQ[key].at === null`).

**سلّم لون «آخر تحديث»** (`agoInfo`) — نفس سلّم `rdyRenderFreshness`:

| المدة | اللون | الزرار |
|---|---|---|
| < ٥ دقايق | أخضر | أزرق |
| < ١٥ دقيقة | أزرق | أزرق |
| < ٦٠ دقيقة | كهرماني | **كهرماني** (`is-stale`) |
| ≥ ساعة | أحمر «الرقم قديم، حدّث» | **كهرماني** |

> 🔴 **`agoInfo` في `shared/shell.js` من v1.5.0 — مش في الصفحة.** كانت
> معرّفة في `index.html` لوحدها، ولما `stats.html` احتاجت نفس اللوحة كان
> الاختيار: نسخة تانية ولا مصدر واحد. النسخة التانية هي بالظبط اللي أنتجت
> باج R1. **ممنوع أي صفحة تعرّفها تاني** — تعريف في صفحة بيغلب الـ shell
> (بيتحمّل قبلها) والسلّمين هيفترقوا في صمت.

> 🔴 **`next` بيتفحص بـ regex قبل التحويل** (`NEXT_RE`). من غير الفحص
> `?next=https://evil…` بيحوّل الموظف لبرّه الهب. الـ regex بيسمح باسم ملف
> محلي + query بس.

## فخاخ كل صفحة — منقولة من الأدوات القديمة

### `print.html` (الطابعة)

- ⛔ **`Promise.all` على دفعة الطباعة ممنوع** — `mapLimit` + `PRINT_CONCURRENCY`
  هو اللي بيمنع فشل الدفعة كلها (R15). الرقم **مش ضبط أداء**.
- 🔴 **القناة هي اللي بتحدد المسار، والتحديد بيتمسح عند التبديل** — ده اللي
  بيمنع الدفعة المختلطة **من أصلها** بدل ما نتحقق منها بعد كده.
  ⚠️ ومن v1.13.0 الصفحة بتفتح **بلا قناة**، والطباعة مقفولة لحد ما تتختار
  واحدة — §CHANNEL-OFF تحت.
- 🔴 **`uiChannel(o)` بتقرا `o.channel` من الـ Worker** وبتزوّد تفرقة **عرض**
  واحدة بس (شو روم عن قاهرة+جيزة). ⛔ ممنوع خريطة زون→قناة تانية في الواجهة —
  خريطتين هيفترقوا مع أول تعديل.
- 🔴 **`allZones: true` لازم يتبعت من `print.html` ومن `index.html` مع بعض** —
  الاتنين بيكتبوا نفس الكاش (`WOC_CACHE_PRINT`). لو واحد بيطلب قناة والتاني
  تلاتة، الرئيسية تقول رقم والصفحة تفتح على رقم تاني.
- **صف بلا قناة بيظهر ومربعه مقفول** — `printable: false` + سبب مكتوب.
  الإخفاء هو اللي أنتج «الأوردر مش ظاهر» مع `ZONE_FILTER`.
- **`toggleSelectAll`/`selectAllVisible`/`printAll` كلهم بيفلتروا على
  `printable`** — وإلا «تحديد الكل» بيفضل مش متعلّم للأبد.
- `getAccessToken` متكاش — متلغيهوش، الفرع الصح `invalidateAccessToken()` على 401.
- `ZONE_FILTER` بيفلتر — والأوردر اللي مالوش `zone` **بيتستبعد هو كمان**.
- `Delivered` حالة نهائية · `/track` بيرجّع ٣ حالات، **الأصفر مش نجاح**.
- ألوان الفاتورة حرفية — استثناء موثّق من #35، مستند الـ iframe مالوش توكنز.
- **`.sticky-header-wrap` z-index 300 و`.tab-chip-menu` 310** — أعلى من
  «محتوى ≤ 200» المكتوبة في Step 9، و**ده مقصود**: قوايم الفلاتر عند 200
  ولازم الهيدر الثابت يقعد فوقها. السلّم كامل: قوايم 200 < الهيدر الثابت 300
  < chip menu 310 < المودالات 600 < التوست 9999. **متنزّلهاش لـ 200** —
  الهيدر هيروح تحت قوايم الفلاتر.
- **Chip التاب الحالي في `extras` بتاع `wocHeader`** — كان جوّه الهيدر القديم،
  و`buildTabChipMenu` كانت بترمي على كل تحميل لما الهيدر اتشال. **الباج ده
  حصل فعلاً واتصلح.**

#### 🔁 §REPRINT — إعادة طباعة أوردر خرج من الطابور (v1.7.0 · قرار أحمد 06-09-2026)

طابور الطباعة بيعرض **حالات «مؤكَّد» بس**، وأوردر اتطبع خلاص حالته `Ready` —
يعني **مش في الجدول بحكم التعريف**. زرار «إعادة 🖨» في `pack.html` كان بيفتح
الصفحة دي مفلترة على أوردر مش موجود فيها. التفاصيل الكاملة والقرارات في
`Order-Printer/CLAUDE.md` §«إعادة طباعة أوردر خرج من الطابور» — الملخص:

- لما `?order=` مايطابقش أي صف، الصفحة بتنادي `POST /lookup` وبتطلّع كارت
  **«🔁 إعادة طباعة الفاتورة»**. الأوردر اللي **في** الطابور بيتفلتر زي ما هو
  ومفيش كارت.
- 🔴 **الصف بيتبني بنفس شكل صف `/orders` بالحرف** وبيعدّي على **نفس**
  `runPreprintGate` و**نفس** `executePrint`. ⛔ ممنوع مسار طباعة تاني — بوابتين
  مراجعة يفترقوا مع أول تعديل (درس R1).
- 🔴 **الحالة مابتتغيّرش.** `/track` عنده فرع صريح (`statusBefore === target` →
  `skipped: 'same_value'`). اللي بيتحدّث: التاج + **ميتافيلد وقت الطباعة بالوقت
  الجديد** + صف جديد في D1 باسم الموظف.
- 🔴 **وقت الطباعة بيتدهس عن قصد** — الثمن: عمود «الوقت منذ الطباعة» في طابور
  التغليف بيرجع 🟢. الكارت بيقول ده **قبل** الضغط، ومش مسموح يتشال من الكارت.
- 🔴 **`o.reprint` في `runPreprintGate`** بيضمن إن أوردر إعادة الطباعة يقع في
  🟡 «سبق طباعته» **على الأقل** حتى لو السجل مالوش صف (`LOGS_FETCH_MAX` بيقص،
  والصفوف قبل v2.0.0 مفيهاش موظف). من غير الشرط ده بيعدّي `clean` بلا مراجعة.
- 🔴 **`o.forceType` بيتبعت لـ `/invoice` من صف إعادة الطباعة بس.** أوردر S2
  حالته `Ready` بيتقري `S1` في القاعدة الافتراضية بتاعة الـ Worker، والنوع ده
  بيختار **بنود الفاتورة**. تمريره على صفوف الطابور كان هيغيّر سلوك قائم.
- ⚠️ **`applyOrderParam` بقت تتنادى بعد `allSettled([queue, logs])`** مش عند
  التحميل — القرار جوّاها «الأوردر ده في الطابور ولا لأ؟»، ومن غير الطابور
  الجواب دايمًا «لأ». وسجل الطباعة هو اللي بيدي «اتطبع كام مرة» للبوابة.
- ⚠️ **الكارت برّه `#printDashboard`** — لازم يفضل شغّال حتى لو جلب الطابور فشل.
- ⚠️ **السطران (`o.reprint` · `o.forceType`) مكرّرين في `Order-Printer/index.html`**
  عن قصد — الكود ده متطابق حرفيًا بين الملفين والفرق الوحيد المسموح `gateLink`.



#### 🚚 قناة بوسطة (قرار أحمد 07-09-2026)

`custom.zone = Other_Regions` معناه **بوسطة**، وكان `ZONE_FILTER` بيستبعده
من `/orders` في صمت — يعني **أكبر قناة في المتجر** (٩٬٠٨٣ مقابل ٧٬١١٣
لـ `Cairo+Giza`) مالهاش طابور طباعة أصلاً. التفاصيل الكاملة في
`Order-Printer/CLAUDE.md` §«قناة بوسطة» — والملخص هنا:

- **مبدّل قناة أحادي** فوق الطابور، والتبديل **بيمسح التحديد**.
  السبب مش تبسيط كود: الورق (ملصق حراري مقابل A4) والطابعة والحارس
  ونافذة النتيجة **كلهم مختلفين**.
  ⚠️ **من v1.13.0 الصفحة بتفتح بلا قناة مختارة** والجدول بيعرض الكل —
  و**الطباعة مقفولة** لحد ما تتختار واحدة (§CHANNEL-OFF تحت). «أحادي»
  لسه ساري: مستحيل يبقى فيه قنتين مختارين، والدفعة المختلطة ممنوعة زي
  ما كانت.
- **الزون قرار توجيه مش جغرافيا** — ١٨٪ من عناوين القاهرة/الجيزة بتروح
  بوسطة (قياس حي)، فالموظف مايقدرش يعرفها من العنوان. عشان كده القناة
  **معروضة صراحةً** في عمود ومبدّل، والاشتقاق من العنوان **ممنوع**
  (قاعدة ١٦ في اللايف سايكل).
- **أوردر بوسطة بياخد بوليصة بس — من غير فاتورة.** مفيش `/invoice` في
  المسار ده خالص.
- **الطباعة بتحوّل الأوردر لـ `Ready`** بنفس مسار الفاتورة: تاج
  `Printed(S1)` + `printing_time_s1` + الحالة.
- 🔴 **«الماكينة» ≠ «المستند»:** `/track` بياخد `type: 'S1'` (بتحكم التاج
  والميتافيلد والحالة) و`doc: 'AWB'` (بتحكم `type` في D1 بس). الخلط بينهم
  بيكتب تاج `Printed(AWB)` وميتافيلد `printing_time_awb` — **الاتنين
  مابيفشلوش**، و`isPrinted` يفضل `false` للأبد وأداة التغليف ماتشوفش وقت
  طباعة. صفر خطأ في الكونسول.
- 🔴 **`latestAWBPrintDate` للعرض بس — ممنوع يدخل أي حارس.** بوسطة بتحدّثه
  مع **كل** جلب للبوليصة حتى لو الورقة ما خرجتش من الطابعة، يعني الأداة
  **بتلوّثه بنفسها**. حارس إعادة الطباعة بيقرا من **سجل D1 لوحده** — نفس
  `REPRINT_WARN_MINUTES` بتاع قاهرة+جيزة.
- ⛔ **الـ PDF بيتطبع زي ما جه بايت ببايت** — مفيش إعادة رسم ومفيش scaling
  ومفيش `@page` مننا. الملف بيطلع مظبوط على الملصق الحراري من داشبورد
  بوسطة، وأي تدخل بيكسر ضمانة قايمة.
- 🔴 **بوابة المراجعة سؤال واحد لأوردر بوسطة من v1.18.0** (قرار أحمد
  09-09-2026): «قطعت الورقة القديمة؟». سؤال «عدّلت الشحنة على بوسطة؟»
  **اتشال** — الفعل ده مش شغل محطة الطباعة، وتعديل الشحنة مكانه داشبورد
  بوسطة. السبب اللي رجّع الأوردر للطابور **مش مهم للمخزن** زي ما كان.
  - ⚠️ `gateSyncState` بتفحص **كل مربع** مش كل صف — البند فضل ساري رغم
    إن كل أوردر بقى بمربع واحد: الدفعة فيها أوردرات كتير.
  - 🔴 **و`bostaUpdated` ما بقاش يتبعت خالص** مع شيل سؤاله. الـ Worker
    لسه شايله في `GUARD_KEYS` (القايمة بيضا مقفولة — مفتاح مش فيها بيتسقط
    في صمت)، بس الواجهة مابتكتبوش: **إقرار ما اتسألش عنه ومكتوب في D1 =
    كذب في السجل**. لو السؤال رجع، السطر يرجع معاه — مش قبله.
- **فرق التحصيل مُشغِّل للبوابة مش حارس تاني** — `cod` عند بوسطة ≠
  `currentTotalPriceSet`. قيمته إنه بيمسك الحالة اللي **الحالة نفسها
  مابتقولش عنها حاجة**: أوردر لسه `Confirmed` عادي والرقم على الملصق غلط.
- **S2 على بوسطة مخفية مؤقتًا** بقرار. ⚠️ **عددها ما بقاش معروض** من
  v1.12.0 — الشريط الأصفر اتشال بالكامل (§BOSTA-GATE تحت). اللي بيمنع
  الرقم من إنه يكدب هو إن **البادجات بتعدّ المعروض فعلاً**، مش وجود شريط.
- **الأوردر اللي ما اترفعش على بوسطة بيتقال بالاسم** وبيتسجّل `not_found` —
  ده الأثر الوحيد لتخطّي بوابة رفع إلزامية.

> 🔴 **أثر جانبي متوقّع يوم النشر:** أوردرات بوسطة هتبدأ تتحوّل لـ `Ready`،
> و**طابور «جاهز للتغليف» بيتبني على `Ready`**. يعني عدّاد التغليف على
> الشاشة الرئيسية و`pack.html` ممكن يكبروا بالقناة الأكبر في المتجر.
> ⚠️ ومسألة «أوردر متغلّف بيفضل عالق في الطابور لو اتطبع تاني» بقت بتنطبق
> على **إعادة طباعة البوليصة** كمان.

#### 🚚 §BOSTA-GATE — طابور بوسطة بيعرض اللي اترفع بس (v1.11.0 · قرار أحمد 07-09-2026)

الرفع على داشبورد بوسطة **بوابة إلزامية قبل الطباعة**
(`ecommoda-order-lifecycle` → `zone-routing.md` §2.2)، وتاج
**`Bosta_Uploaded_S1`** هو أثرها الوحيد على شوبيفاي. من غير الحارس ده كان
طابور بوسطة بيعرض أوردرات **مستحيل تتطبع بوليصتها**: الموظف يحددها ويدوس
طباعة، والدفعة ترجّع صف أحمر `not_found` لكل واحدة (وصف `not_found` في D1).

- 🔴 **الفلترة في الواجهة مش في الـ Worker** — `tags` بترجع من `/orders`
  أصلاً (نفس مصدر `isPrinted`). صفر تعديل Worker · صفر Promote · صفر ترفيع
  لـ `printer.min` للبند ده.
- 🔴 **البوابة في `shared/shell.js` §BOSTA-GATE — مش في الصفحة** (اتنقلت في
  v1.11.1). `BOSTA_UPLOADED_TAG` · `wocBostaUploaded` · `wocChannelOf` ·
  `wocChannelGate` كلهم هناك، و`index.html` و`print.html` بيعدّوا منهم
  الاتنين. **ده السبب اللي البوابة نزلت الـ shell عشانه:** v1.11.0 حطّت
  الحارس في `print.html` بس، فالرئيسية فضلت بتعدّ بالقاعدة القديمة وقالت
  «بوسطة ٦٦» و«جاهز للطباعة ٨٤» بينما الصفحة بتفتح على ٦ — بالظبط الخطر
  المكتوب فوق في «`allZones` لازم يتبعت من الاتنين». ⛔ ممنوع أي صفحة
  تعرّفهم تاني: تعريف في صفحة بيغلب الـ shell (بيتحمّل قبلها) والقاعدتين
  هيفترقوا في صمت (درس R1).
- 🔴 **وخريطة زون→قناة بقت نسخة واحدة كمان** — `uiChannel` اتشالت من
  `print.html` والنسخة المكرّرة اتشالت من `index.html`، والاتنين بينادوا
  `wocChannelOf`. القاعدة اللي كانت مكتوبة «⛔ ممنوع خريطة تانية» بقت
  **متنفَّذة** مش مكتوبة بس.
- ⚠️ **حارس عرض مش حارس أمان.** الحارس الحقيقي لسه `bosta_lookup` (بيرجّع
  `not_found`). التاج بيوفّر الرحلة الضايعة بس — وعشان كده **مش مطبَّق على
  كارت إعادة الطباعة**: ده فعل مقصود على أوردر بعينه، والبوابة الحقيقية
  بتقول لأ لو مش هناك.
- 🔴 **شريط «الغياب معلوم» اتشال في v1.12.0 (قرار أحمد 07-09-2026).** كان
  بيعرض عدد اللي لسه ما اترفعش **باسم التاج مكتوب**، وعدد S2 المخفية،
  و§JUST-PRINTED. الشريط كله اتشال بقرار — ومعاه `renderChannelNote`
  و`chanHiddenS2`/`chanHiddenNew`.
  ⚠️ **الثمن لازم يتقال:** الطابور دلوقتي بيبان أقصر **من غير سطر يقول ليه**.
  اللي فاضل بيحمي من إن ده يبقى كذب حاجتان بس: **بادج القناة وبادج «طباعة
  الكل» بيعدّوا اللي بيتعرض فعلاً** (`wocChannelGate` — نفس البوابة بالحرف)،
  وصف «بلا قناة» بيفضل **ظاهر ومقفول وسببه مكتوب جنبه في الجدول**. يعني
  الرقم مابيكدبش، بس **السبب مابقاش مكتوب** لحالة «لسه ما اترفعش على بوسطة».
  ⛔ **متضيفش نسخة تانية من الشريط في صفحة تانية** — لو الغياب ده رجع يتقال،
  مكانه سطر واحد مشترك مش نسخة في كل صفحة (درس R1).
- 🔴 **البادج بيعدّ المعروض فعلاً — مش القناة كلها (اتصلح في v1.11.1).**
  v1.11.0 سابت البادج على القناة كلها (نمط `chanHiddenS2`) والفرق متفسَّر في
  الملاحظة، فطلع **بادج ٦٦ فوق جدول فيه ٦**. الدرس: **رقم على زرار مش
  بيتقرا كحاشية — بيتقرا كوعد بشغل موجود**، والموظف بيضغط عليه عشان الرقم
  مش عشان الملاحظة تحته. المستبعَد بيتقال بعدده في الملاحظة، **مش في
  العدّاد**. وفيه **تلات** بنود في فحص المتصفح بيقفلوا الرجوع: بادج القناة
  == عدد صفوف الجدول · بادج القناة == عدّاد النتائج · **بادج «طباعة الكل»
  == عدد صفوف الجدول** (البند التالت اتضاف في v1.12.0 لما الشريط اتشال —
  البادج بقى المصدر الوحيد للرقم).
- ⚠️ **قياس يوم النشر (07-09-2026): صفر من ٦٠ أوردر في الطابور عليه التاج**،
  والتاج موجود على **أوردرين في المتجر كله** — الاتنين اتطبعوا بالفعل. يعني
  الطابور هيبان **فاضي** لحد ما أداة الرفع تبدأ تكتب التاج. ده **السلوك
  الصح** (الأوردرات دي فعلاً مش على بوسطة)، بس لازم يكون متوقّع.

#### 🖨️ §PRINT-BAR + §AWB-TAB — تمريرة واجهة (v1.12.0 · قرار أحمد 07-09-2026)

خمس تغييرات في `print.html` بس. **صفر تعديل Worker · صفر Promote · صفر ترفيع
لأي `min`** — كلها عرض وتفاعل.

| # | التغيير | السبب |
|---|---|---|
| ① | **قسم الفلاتر مطوي افتراضيًا** (`fltOpen:false` في التابين) | الطابور هو الشغل؛ الفلاتر كانت بتاخد نص الشاشة الأولى قبل أول صف |
| ② | **الشريط الأصفر اتشال بالكامل** (`chan-note` · `renderChannelNote`) | قرار أحمد — الثمن مكتوب في §BOSTA-GATE فوق |
| ③ | **مربعا «عادي / استبدال-استرجاع» اتشالوا** (`updatePrintKPIs`) | عدّاد القناة فوقهم بيقول نفس الرقم بدقة أعلى |
| ④ | **شريط الطباعة اتعاد تصميمه** — زراير في النص، أكبر، وبادج بالعدد على كل واحد | الرقم بقى **على** الزرار بدل سطر جنبه |
| ⑤ | **البوليصة بتتفتح في تاب جديد** بدل `iframe` بارتفاع 460px | الموظف كان بيدوس «افتح في تاب» في كل مرة فعليًا |

- 🔴 **بادج «طباعة الكل» بيعدّ `filteredOrders.filter(o => o.printable)`** —
  بالحرف نفس الفلتر اللي `printAll()` بتشتغل بيه. أي فرق بين الرقمين =
  رجوع باج v1.11.0 (بادج ٦٦ فوق جدول فيه ٦)، وفيه بند في فحص المتصفح بيقفله.
- 🔴 **التلات زراير بيتعطّلوا ما بيختفوش.** زرار بيظهر ويختفي بيزحلق اللي
  جنبه تحت إيد الموظف وهو بيدوس — والدوسة الغلط هنا معناها دفعة ورق.
- 🔴 **تاب البوليصة بيتحجز في `startPrintAWB` قبل أي `await`** (`preopenAwbTab`)
  ومسار بوسطة كله اتلفّ في `runPrintAWB` جوّه `try/finally`. السبب:
  `window.open` بعد `await` بيتمنع كـ popup — كروم بيسحب صلاحية فتح النوافذ
  بعد ثواني من آخر ضغطة، والدفعة بتاخد أطول من كده على بوسطة. يعني المنع
  كان **هيحصل في اليوم العادي مش في حالة نادرة**.
  ⚠️ و`closeAwbWin()` في الـ `finally` بتقفل التاب المحجوز لو الدفعة وقعت
  أو اتلغت — و**بلا أثر** لو التاب اتستهلك (`awbWin` بيتصفّر وقت التوجيه).
- 🔴 **والحجز ده بيتقفل ويترجع لو البوابة اتفتحت** (v1.18.0 · قرار أحمد
  09-09-2026). `runPreprintGate` بقت بتاخد خطّافين من مسار بوسطة:
  `onOpen: closeAwbWin` بتتنادى **قبل** ما النافذة تظهر، و
  `onConfirm: preopenAwbTab` **جوّه ضغطة زرار الطباعة نفسها**.
  ⚠️ **الباج اللي البند اتكتب عشانه:** شاشة «🚚 جاري تحضير البوليصة من
  بوسطة…» كانت بتتفتح في تاب جنب الموظف **وهو لسه ما أقرّش بقطع الورقة**،
  وبتفضل قاعدة طول ما النافذة مفتوحة — يعني الشاشة بتقول إن الطباعة بدأت
  وهي ما بدأتش. ⛔ ممنوع الرجوع لحجز واحد في أول المسار.
  ⚠️ والحجز المتأخر **مضمون مش مخاطرة**: ضغطة زرار البوابة بتجدّد صلاحية
  فتح النوافذ، والحجز بيحصل قبل أي `await` جوّه `finish`. ومسار **بلا
  بوابة** ما اتغيّرش ولا سطر — الحجز الأول بتاعه لسه في مكانه.
  ⚠️ وفيه بند في فحص المتصفح بيقفل على **الترتيب** نفسه
  (`preopen → close → preopen → nav`) مش على وجود الخطوات.
- 🔴 **`window.open` من غير `noopener`** — مع `noopener` الدالة بترجّع `null`
  حتى وهي نجحت، فمستحيل نفرّق بين نجاح ومنع. الملف بلوب من نفس الـ origin
  فمفيش مكسب أمني من الحجب هنا.
- 🔴 **الشريط الاحتياطي (`#awbFallback`) مش رفاهية** — لو المتصفح منع الفتح
  خالص، لازم يبقى فيه زرار بضغطة مباشرة. **بوليصة ما خرجتش من غير ما حد
  يعرف = شحنة بتخرج من المخزن من غير ملصق.**
- ⛔ **الـ PDF لسه بيتفتح زي ما جه بايت ببايت** — التاب الجديد ما غيّرش ده:
  مفيش إعادة رسم ومفيش scaling ومفيش `@page` مننا.

#### 🖨️ §COUNT-BOX — تمريرة واجهة (v1.14.0 · قرار أحمد 08-09-2026)

خمس تغييرات في `print.html`. **صفر تعديل Worker · صفر Promote · صفر ترفيع لأي
`min`** — كلها عرض.

| # | التغيير | السبب |
|---|---|---|
| ① | اسم الأداة بقى **«قسم الطباعة»**، وكلمة «القناة» بقت **«نوع الفاتورة»** فوق المربعات وفي عمود الجدول | توحيد التسمية مع «قسم التغليف» |
| ② | **صندوق العدّ** فوق الطابور: العدد الكلي · المربعات **في النص** · لوحة «آخر تحديث» + زرار تحديث ببادج | **نسخة من `.count-box` في `pack.html`** بالحرف — الموظف بيتنقّل بين الصفحتين طول اليوم |
| ③ | ختم «آخر تحديث» وزرار 🔄 **اتشالوا من الهيدر** | كانوا **ختم بلا عمر**: طابور اتجاب من ٤٠ دقيقة شكله زي واحد اتجاب من دقيقة، والموظف بياخد قرار طباعة على الرقم ده. وسيبهم في الاتنين = ختمين مختلفين على نفس الشاشة |
| ④ | عمودا **«التاريخ»** و**«الوقت»** بقوا عمود واحد **«تاريخ الأوردر»** ببادج عمر · وعمود **«النوع»** بقى **«نوع الأوردر»** نص ملوّن | **نفس عمودَي `pack.html`** بالحرف. ساعة إنشاء الأوردر مش معلومة المخزن بيتصرّف عليها — اللي بيفرق «بقاله قد إيه» |
| ⑤ | سطر «اختر قناة» (`#chanHint`) **اتشال** | قرار أحمد — الثمن مكتوب تحت في §CHANNEL-OFF |

- 🔴 **الرقم الكبير وبادج «تحديث» = مجموع القنوات الأربعة** — «اللي ينفع يتطبع
  دلوقتي» بالحرف، ونفس رقم صف «جاهز للطباعة» في الشاشة الرئيسية بالظبط. ومجموعه
  **بيساوي عدد صفوف الجدول** بلا قناة مختارة، لأن كل صف بيعدّي على بوابة قناته
  هو في الحالتين. رقمان مختلفان على نفس الشاشة أسوأ من رقم واحد ناقص —
  وفيه بندان في فحص المتصفح بيقفلوا الرجوع.
- 🔴 **كلاسات اللوحة (`.woc-fresh` · `.woc-ago` · `.woc-stamp-chip` · `.woc-rbtn`)
  من `shared/shell.css` — مش نسخة في `print.html`.** اللوحة دي في **تلات**
  شاشات دلوقتي (الرئيسية · التغليف · الطباعة)، ونسخة CSS رابعة معناها إن الشكل
  يفترق مع أول تعديل (درس R1). و`agoInfo` في الـ shell أصلاً.
- ⚠️ **`flex:1` على `.chan-chips` هي اللي بتتوسّط المربعات**، والأكشن بيتزحلق
  للطرف من `margin-inline-start:auto` — نفس فخّ `.woc-row-mid` في الرئيسية:
  `flex:1` على منطقة تانية بيوقّع الأكشن في نص الصندوق.
- ⚠️ **بادج عمر الأوردر بيتحدّث في مكانه** (`data-prt-age` + نبضة ٣٠ ثانية،
  **صفر نداء شبكة**) — إعادة رسم الجدول كانت هتصفّر التمرير تحت إيد الموظف كل
  نص دقيقة (نفس `rdyTick` في التغليف).
- 🔴 **`wocDayLevel`/`wocOrderAge` في `shared/shell.js` §HELPERS — مش في الصفحة**
  (اتنقلوا من `pack.html` في v1.14.0، و`rdyDayDiff`/`rdyDayLevel`/`rdyOrderAge`
  بقوا **تفويض**). الصفحتان بيعرضوا نفس البادج على نفس الأوردر، ونسختين معناهم
  إن نفس الأوردر يبان «منذ يومين» هنا و«منذ ٣ أيام» هناك. ⛔ ممنوع تعريف محلي.
  ⚠️ بس **CSS `.time-badge` مكرّر في الصفحتين** (قاعدة الأسبقية) — الدرجة من
  الـ shell فمستحيل تفترق، اللون هو اللي مكرّر. أي تعديل عليه في الصفحتين في
  نفس التمريرة.
- ⚠️ **`typeBadge` لسه مستخدم في تاب السجل** — `typeText` للطابور بس. هناك
  العمود جنب أعمدة نصية قليلة فالبادج بيساعد؛ هنا الجدول تمن أعمدة.
- ⚠️ **`sortConfig.time` بيفضل** رغم إن عمود «الوقت» اتشال من الطابور — تاب
  السجل لسه عنده العمودين منفصلين.
- ⚠️ **«بلا قناة» فضلت باسمها** في المربع وفي عمود الجدول — دي **حالة** مسمّاة
  في `CHANNELS` وفي `wocChannelOf` ومربوطة بـ `lockReason`، مش ليبل عرض.

#### 🔴 §CHANNEL-OFF — الصفحة بتفتح بلا قناة مختارة (v1.13.0 · قرار أحمد 07-09-2026)

`activeChannel` كان بيبدأ `"invoice"`، فمربع «فاتورة — قاهرة+جيزة» بيبقى
**مولّع من غير ما حد يدوس** والجدول بيعرض قناة واحدة وكأنها كل الطابور.
دلوقتي بيبدأ **`null`**: مفيش مربع مولّع · الجدول بيعرض القنوات كلها ·
الضغط بيفلتر · ضغطة تانية على نفس المربع بترجّع الكل — **نفس سلوك مربعَي
«بوسطة/مناديب» في `pack.html`** (`.zchip` بعلامة `✕`) بالحرف.

🔴 **بس فيه فرق واحد إلزامي عن التغليف — ومش مسموح يتشال.** مربعات التغليف
فلتر عرض بحت؛ القناة هنا **بتحدد مسار الطباعة**: `/invoice` (فاتورة A4)
مقابل `bosta_awb` (بوليصة على ملصق حراري)، ومعاها الطابعة والحارس ونافذة
النتيجة. عشان كده:

| الحالة | الجدول | «طباعة الكل» و«طباعة المحدد» | البادج |
|---|---|---|---|
| `activeChannel === null` | **كل القنوات** | **متعطّلين** | **`—`** |
| قناة مختارة | القناة دي بس | زي ما كانوا | رقم |

- 🔴 **البادج `—` مش رقم لما مفيش قناة.** رقم على زرار **متعطّل** بيتقرا
  وعد بشغل والموظف بيدوس عليه — والرقم ده مجموع تلات قنوات بتطبع على ورق
  مختلف، يعني وعد **مستحيل ننفّذه أصلاً**. نفس درس بادج ٦٦ فوق جدول فيه ٦
  (v1.11.1).
- 🔴 **سطر «اختر قناة» (`#chanHint`) اتشال في v1.14.0 بقرار أحمد.** كان
  **سبب زرار متعطّل** مش شريط ملاحظات، وبيختفي أول ما تتختار قناة.
  ⚠️ **الثمن لازم يتقال:** «طباعة الكل» و«طباعة المحدد» بيفتحوا **رماديين من
  غير سطر يقول ليه**. اللي فاضل بيمنع ده من إنه يتقري عطل حاجتان: **البادج
  بيقول `—` مش رقم** (زرار متعطّل برقم عليه بيتقرا وعد بشغل)، و**مربعات نوع
  الفاتورة كلها مطفية** — يعني الحالة ظاهرة، بس **السبب مابقاش مكتوب**.
  ⛔ متضيفش نسخة تانية منه في صفحة تانية لو رجع — سطر واحد مشترك (درس R1).
- 🔴 **كل صف بيعدّي على بوابة قناته هو** حتى في وضع «الكل»
  (`wocChannelGate(o, o.chan)`) — **مش** بوابة مفتوحة. يعني أوردر بوسطة لسه
  ما اترفعش يفضل مستبعَد زي ما هو، و**مجموع بادجات القنوات == عدد صفوف
  الجدول** بالظبط. فيه بند في فحص المتصفح بيقفل ده.
- 🔴 **التحديد بيتمسح عند أي تغيير في القناة — والقفل كمان.** التحديد اتعمل
  تحت قناة، وشيلها معناه إن الدفعة مابقاش ليها مسار. ده اللي بيمنع الدفعة
  المختلطة **من أصلها** بدل ما نتحقق منها بعد كده.
- ⚠️ **تلات حراس مش واحد:** `disabled` على الزرار · فحص `activeChannel`
  في `printAll` · وفحص تاني في `startPrint`. التالت مش زيادة — `startPrint`
  بتتنادى من `printAll` ومن الزرار ومن أي مسار جديد يتضاف بعدين.
- ⚠️ **`startReprint` ما اتلمستش** — كارت إعادة الطباعة بيقرا `row.chan`
  (قناة الأوردر نفسه) مش `activeChannel`، فهو شغّال بلا قناة مختارة زي ما
  كان بالظبط.

#### 🧾 §LAST-DOC — «آخر طباعة: فاتورة ولا بوليصة؟» (v1.18.0 · قرار أحمد 09-09-2026)

«اتطبع قبل كده» **مش كفاية** — لازم يتقال **إيه** اللي اتطبع. الحالة الواقعية:
أوردر بيتطبعله فاتورة مناديب، وبعدين يتكشف إن العنوان هيخرج مع بوسطة فالزون
بيتغيّر (أو العكس) — فالورقة اللي في المخزن **من نوع تاني خالص** عن اللي
هيتطبع دلوقتي. من غير الشارة دي الموظف بيقرا «آخر طباعة من يومين» ويفتكر إن
عنده نفس الورقة.

- 🔴 **المصدر سجل D1 لوحده** (`printLastDocMap` من `/logs`، اللي بيرجّع
  `type` = `S1` · `S2` · `AWB`). ميتافيلد `printing_time_s1` **مابيقولش نوع
  المستند**: بوسطة بتكتب فيه بنفس مفتاح الفاتورة بالظبط (`doc` منفصل عن
  `type` — فوق في «الماكينة ≠ المستند»). فأوردر أقدم من السجل أو خارج سقفه
  بيفضل **«مش معروف»** ومفيش شارة أصلاً — **«معرفناش» لازم تفضل فاضية،
  وشارة غلط أسوأ من مفيش شارة**.
- 🔴 **التحذير مشروط.** الشارة رمادية على كل صف سبق طباعته، وبتولّع
  **كهرماني** بس لما نوع آخر ورقة يخالف قناة الأوردر دلوقتي. تحذير على
  الاتنين = لون بلا معنى (نفس قاعدة `.tool-danger`).
- ⚠️ **المقارنة على المستند مش على اسم القناة** — «مناديب» و«شو روم»
  الاتنين بيطبعوا **فاتورة**، فالانتقال بينهم مش تغيير في الورق ومايستاهلش
  تحذير. `DOC_KIND` بيحوّل `S1`/`S2` → `invoice` و`AWB` → `awb`.
- ⚠️ الشارة في **تلات أماكن**: الجدول · صف البوابة · كارت إعادة الطباعة —
  ومصدرها دالة واحدة (`lastDocChip`). نسخة تانية للنص أو للشرط هتفترق
  مع أول تعديل (درس R1).
- ✅ **صفر تعديل Worker · صفر Promote · صفر ترفيع لأي `min`** — `type`
  بيرجع من `POST /logs` أصلاً (نفس النداء اللي بيبني عدّاد «مرات الطباعة»).

#### 🏷️ §JOB-TITLE — اسم مهمة الطباعة (v1.18.0 · قرار أحمد 09-09-2026)

صيغة **واحدة** للفاتورة والبوليصة: `09-09-2026 - 11.51 - عدد 3 - بوسطة`
(`printJobTitle(count, chan)`). القناة في الآخر لأن الموظف بيطبع من التلاتة
في نفس الوردية — من غيرها ملفين اتحفظوا في نفس الدقيقة بنفس العدد
**مايتفرّقوش**.

- ⚠️ الوقت **بتوقيت القاهرة محسوب** (`toCairo`) مش ساعة الجهاز — لابتوب
  بتوقيت غلط كان هيسمّي الملفات غلط في صمت.
- 🔴 **البوليصة بتاخد الاسم من جوّه ملف الـ PDF نفسه** (`pdfWithTitle`).
  التاب بيتوجّه لـ blob، والـ blob مالوش اسم ملف — فالعنوان بيتحط في خانة
  **`/Title`** جوّه الملف بـ **incremental update**: كائن جديد + xref جديد
  **بعد** `%%EOF` القديم، وبايتات الملف الأصلية **مابتتلمسش ولا بايت**.
  ⛔ يعني قاعدة «الـ PDF بيتطبع زي ما جه» **ما اتكسرتش**: التاب لسه **ملف
  PDF مفتوح مباشرةً** مش صفحة غلاف بتاعتنا فيها إطار، فـ `Ctrl+P` وزرار
  الطابعة وقايمة المتصفح كلهم لسه بيطبعوا بايتات بوسطة.
  ⚠️ **البديل المرفوض كان صفحة غلاف** — بتدّي تحكّم كامل في العنوان، بس
  بتحطّ مستند بتاعنا في مسار الطباعة: `Ctrl+P` بيطبع الغلاف على A4
  والملصق بيطلع مقصوص، وقايمة المتصفح ⋮ **مش قابلة للاعتراض**.
- 🔴 **أي شك = الملف الأصلي.** كل مسار فشل في `pdfWithTitle` بيرجّع
  البايتات زي ما هي (ملف مشفّر · `startxref` مش موجود · xref مش مفهوم ·
  أي رمي) — **بوليصة ما خرجتش أسوأ بمراحل من تاب اسمه blob URL**.
- ⚠️ **الشكلان مدعومان**: جدول xref قديم و**xref stream** (PDF 1.5+)، وكل
  واحد بيتكمّل **بنفس شكله**. جدول قديم بـ `/Prev` على ستريم = ملف هجين
  وسلوك القرّاء عليه مش مضمون. الاتنين متأكّدين على `pdf-lib` و`pdf.js`.
- ⚠️ العنوان **هيكس UTF-16BE بـ BOM** — سترينج `(…)` عادي بيتقرا latin1
  والعربي بيطلع رموز مكسّرة.
- 🟡 **بند مفتوح: لسه ما اتأكّدش على بوليصة بوسطة حقيقية.** عارض الـ PDF
  بتاع كروم مش شغّال في هيدلس، فبند الفحص بيقرا الخانة من الملف نفسه —
  **مش** من عنوان التاب. ولو الملف عنده XMP فيها `dc:title` فيه احتمال
  العارض يفضّلها. الأثر لو حصل: الاسم مايتغيّرش وبس، **صفر أثر على الطباعة**.
- ⚠️ **اسم الملف المحفوظ حاجة تانية** — زرار «حفظ» بتاع العارض بياخد الاسم
  من الـ URL، والـ blob اسمه UUID. `/Title` بيتحكم في **عنوان التاب** بس.

#### 🔴 §JUST-PRINTED — الأوردر المطبوع بيختفي فورًا (v1.11.0)

`/orders` بيفلتر بـ `metafields.custom.manual_status` عبر **فهرس بحث
شوبيفاي**، والفهرس ده **مش فوري**. الطباعة بتحوّل الحالة لـ `Ready` والتحديث
بيتنادى بعدها على طول — فالرد بيرجّع الأوردر المطبوع **وهو لسه في الطابور**
لثواني. الموظف بيشوفه قدامه بعد ما طبعه فيفتكر إن الطباعة ما اتسجّلتش.

- 🔴 **الشيل المحلي لوحده مش كفاية** — التحديث اللي بعده بيرجّعه والصف بيرفرف.
  الاستبعاد **بيعيش عبر التحديثات** لحد ما الـ Worker يبطّل يرجّعه (= الفهرس لحق).
- 🔴 **مش تفاؤل ومش تخمين:** الأوردر مابيتشالش إلا لما شوبيفاي **تأكّد** إن
  حالته بقت `Ready` — `statusAfter` من `/track` (Worker v2.6.0)، و
  `setStatusToReady` بتقرا القيمة اللي رجعت من `metafieldsSet` وبترمي لو مش
  مطابقة. طباعة رجعت `warning` عشان الحالة فشلت = الأوردر **بيفضل في الطابور**.
- ⚠️ **`printer.min` ما اترفعش** (لسه `2.5.0`) — الهب **مش معتمد** على
  `statusAfter`: من غيره بيرجع لـ `status === 'success'`، وهي أضيق فالتدهور
  آمن (أسوأ حالة السلوك القديم بالحرف). ترفيعه كان هيولّع تحذير كاذب على أي
  rollback مشروع (Standards #29).
- ⚠️ **سقف زمني إلزامي** (`PRINTED_HIDE_MS` = ١٠ دقايق) — لو حصل أي شيء غير
  متوقع وفضل الـ Worker يرجّع الأوردر، بيرجع للطابور بدل ما يتخفي للأبد.
  الاتجاه الآمن دايمًا «يرجع» مش «يفضل مخفي».
- ⚠️ **في الذاكرة بس** زي `activeChannel` — ريفريش الصفحة بيصفّرها، وساعتها
  الفهرس يكون لحق من زمان.
- ⚠️ **الكاش بيتكتب مفلتر** (`WOC_CACHE_PRINT`) — الشاشة الرئيسية بتقرا منه،
  وعدّاد فيه أوردر اتطبع خلاص بيخلّي الرئيسية تقول رقم والصفحة تفتح على رقم تاني.
- ⚠️ **سبب الاختفاء كان بيتكتب في الشريط الأصفر — والشريط اتشال في v1.12.0.**
  يعني الأوردر بيختفي من الطابور **من غير سطر يقول ليه**. متسيبش ده يتقري
  عطل: الاختفاء ده بيحصل **بعد ضغطة طباعة الموظف نفسه** وبنافذة نتيجة
  بتتفتح قدامه فيها الأوردر بالاسم — يعني السياق موجود في النافذة، مش ناقص.
  ⛔ ومربعا KPI (`updatePrintKPIs` · «عادي» و«استبدال/استرجاع») اتشالوا في
  نفس التمريرة — أي رجوع ليهم لازم يعدّي من `applyPrintFilters` مش من الجلب،
  وإلا تبديل القناة بيسيب أرقام القناة اللي قبلها تحت ليبل القناة الجديدة
  (باج قايم اتصلح في v1.11.0).

#### 🔴 حارس الفاتورة اللاغية + بوابة المراجعة (قرار أحمد 06-09-2026)

أوردر رجع للطابور (`Confirmed + Edit` أو `Confirmed`) وعليه أثر طباعة سابقة =
**ورقة بمحتوى قديم موجودة في المخزن**. التفاصيل الكاملة في
`Order-Printer/CLAUDE.md` §«حارس الفاتورة اللاغية» — الملخص:

- الحُرّاس التلاتة بقوا **بوابة واحدة** (`§PREPRINT-GATE`)، وكل أوردر بيتعرض
  **مرة واحدة** تحت أعلى فئة. ⛔ ممنوع ترجع تتقسم لنوافذ متتالية.
- 🔴 فاتورة لاغية = **مربع تأكيد لكل أوردر**. **مفيش «قطعت الكل»** بقرار.
- 📦 `packingTimeS1` موجود = الفاتورة **جوّه طرد** — فتح مش قطع ورقة.
- «اطبع الباقيين» و«إلغاء» **عمرهم ما بيتعطّلوا**، وفشل جلب السجل **مايمنعش الطباعة**.
- الإقرار بيتسجّل في `extra.guard` بتاع صف الطباعة — **من غير `type` جديد** في D1.
- كل فاتورة في آخرها سطر 7px بتاريخ ووقت الطباعة بتوقيت القاهرة واسم الموظف.
- ⚠️ **الكود مكرّر حرفيًا في `print.html` و`Order-Printer/index.html`** — أي
  تعديل لازم يحصل في الاتنين في نفس التمريرة (درس R1). الفرق الوحيد المقصود
  سطر `const gateLink = …` (`prtOrderLink` هنا · `orderLink` هناك).

#### 🔴 تحذير الطباعة المزدوجة (قرار أحمد 05-09-2026) — اندمج في البوابة

المشكلة الفعلية: **جهازين × «طباعة الكل» = ٤٢ ورقة بدل ٢١.**

```js
const REPRINT_WARN_MINUTES = 30;   // ثابت مسمّى — تغييره قرار مش صدفة
```

- **جلب طازة** لسجل يوم القاهرة في كل مرة (`findRecentlyPrinted`). الخريطة
  المبنية وقت الدخول ممكن تكون عمرها ٣ ساعات، و**تحذير مبني على بيانات قديمة
  أسوأ من مفيش تحذير** لأنه بيدّي إحساس كاذب بالأمان. D1 مش شوبيفاي —
  الاستعلام رخيص وبيستخدم `idx_logs_tool_ts`.
- **نافذة واحدة للدفعة كلها** مش سؤال لكل أوردر — سؤال ×٢١ = الموظف بيدوس
  Enter من غير ما يقرا، والحارس يبقى ديكور.
- **الأوردر بيتحدد بالاسم** + الوقت + الموظف — «٣ اتطبعوا» من غير أسماء يعني
  الموظف مايقدرش يقرر.
- **«اطبع الباقيين» هو الأساسي** (`btn-primary`) · **«إلغاء» `btn-red`** (#38).
- **مفيش نافذة لو `recent.length === 0`** — اليوم العادي مايتغيّرش فيه حاجة.
- 🔴 **الفشل مايمنعش الطباعة أبدًا** — `catch` بيكمّل + توست. حارس بيمنع الشغل
  بيتشال بعد أسبوع.
- z-index النافذة **600** زي كل المودالات (Standards #17).

> ⚠️ **فيه حارسان مش واحد.** الحارس القديم (`showConfirmReprint`) بيسأل «سبق
> طباعته **أبدًا**؟» من `startPrint`، والجديد بيسأل «اتطبع خلال آخر ٣٠
> دقيقة؟» من `executePrint`. أوردر اتطبع من ١٠ دقايق بيشوف **الاتنين**
> بالترتيب. ده متساب عن قصد — الأول عن التاريخ والتاني عن الدفعة الجارية.

### `pack.html` (التغليف)

- **`§PACK::evaluatePackGuard` المصدر الوحيد** لكشف «اتغلّف قبل كده».
- **`classifyS2Subtype` و§BOSTA في مكانين** — راجع الخطر المفتوح في
  `Orders-Packing-Checker/CLAUDE.md`. الهب **مابيحلهوش** (في الـ Workers مش
  في الواجهة).
- **«الوقت منذ الطباعة» و«عمر الأوردر» بيتحسبوا بأيام تقويمية** بتوقيت
  القاهرة، مش ٢٤ ساعة.
- **النبضة مالهاش أي نداء شبكة**، وإعادة رسم الجدول فيها بتصفّر التمرير تحت
  إيد الموظف كل نص دقيقة.
- **`rdyZoneInfo` بتتنادى مرة واحدة وقت بناء الصف.**
- **أيقونة الطابعة SVG مش إيموجي** — `🖨` (U+1F5A8) مابيترسمش على أجهزة المخزن.
- **`.entry-grid` = `repeat(3, 1fr)` والألوان التلاتة** — اللون هو التمييز
  الوحيد بين المداخل. أي توحيد بيرجّع «سكنت في المربع الغلط».
- **`lastEntryMode` مش في التخزين المحلي** — موظف جديد على نفس الجهاز يبدأ من
  المربع الافتراضي.
- **`rdyRenderFrom(data, atIso)` هي المصدر الوحيد للرسم** — الجلب الطازة
  والكاش الاتنين بيعدّوا منها. نسخة تانية للكاش هتفترق مع أول تعديل.
- **زراري «خيارات» بقوا صفحات جوّه الهب** بلا `target="_blank"`، ولسه **بلا
  أي أكشن على الأوردر ومفيش نداء للـ Worker** — بيفتحوا صفحة وبس. لو حد ضاف
  أكشن هناك بعدين، لازم يعدّي على حارس التغليف زي أي كتابة تانية.
- **زرار «إعادة 🖨» بيبعت `oid` كمان من v1.7.0** (`o.orderId`) — `print.html`
  بتجيب الأوردر بالـ id مباشرةً بدل بحث بالاسم. الاسم لوحده بيشتغل برضه
  (الـ Worker بيطابق تمامًا)، بس الـ id بيشيل أي التباس ويوفّر استعلام.
  ⚠️ **الزرار لسه لينك — لسه مايطبعش.** الطباعة بتحصل من الكارت في
  `print.html` بضغطة الموظف وبعد بوابة المراجعة.
- **فعل `packed` لسه تحت `pack_checker`** — الدخول بس هو اللي بقى
  `warehouse_ops_center`. التاريخ ما اتكسرش.
- 🔴 **مربعات «بوسطة/شو روم/مناديب» قاعدتهم في `shared/shell.js` §COURIER-GROUP —
  مش في الصفحة** (اتنقلوا في v1.13.0). `wocCourierGroup` و
  `WOC_COURIER_GROUPS` و`wocCourierCounts` كلهم هناك، و`RDY_COURIER_GROUPS`
  و`rdyCourierGroupOf` بقوا **تفويض** ليهم. السبب: الشاشة الرئيسية بقت
  بتعرض **نفس** المربعات على صف «جاهز للتغليف» من **نفس** رد
  `get_ready_orders` — نسخة تانية من القاعدة معناها الرئيسية تقول ٣٦
  والصفحة تفتح على رقم تاني (نفس اللي حصل مع بوابة بوسطة في v1.11.0 · درس R1).
  ⛔ ممنوع أي صفحة تعرّفهم تاني.
  🔴 **والتقسيم بقى تلاتي في v1.14.0** (قرار أحمد 08-09-2026): بوسطة · **شو
  روم** · مناديب. الشو روم كان واقع في «مناديب» رغم إنه **أخضر في عمود
  المندوب**، فالموظف كان بيدوّر على الصفوف الخضرا في مربع مش بتاعها — دلوقتي
  المربع واللون **متفقين**. الشو روم **محطة تسليم مختلفة** (العميل بيستلم من
  الفرع، فمافيش شحن ولا ملصق أصلاً)، والفاضي لسه بيقع في «مناديب» — «مفيش
  مندوب مسجل» مش شو روم، ومربع رابع كان هيدّي مربع بصفر في اليوم العادي.
  ⚠️ والمطابقة **بتشيل المسافات والشرطات والـ underscore**: `Show_Room` و
  `Show Room` و`showroom` و«شو روم» كلهم مربع واحد — مطابقة حرفية على
  `Showroom` بالظبط كانت هتخلّي صف أخضر في العمود يقع في «مناديب».
  ⚠️ `rdyCourierClass` (الرباعي) فضل في الصفحة لأنه بيوصف **العمود** مش
  المربعات — الفرق الفاضل بينهم إن العمود عنده لون رابع لـ«غير محدد».
- 🔴 **`rdyCourierChipRows` بتعدّ من `wocCourierCounts` — مش خريطة محلية**
  (اتصلح في v1.14.0). خريطة بمفاتيح مكتوبة بالإيد معناها إن مجموعة جديدة
  بتدخل `WOC_COURIER_GROUPS` وتطلع مربع **عدّاده `undefined`** من غير أي
  خطأ في الكونسول.
- ⚠️ **ليبل «أوردرات جاهزة للتغليف» وبادجا «عادي/استبدال» اتشالوا من صندوق
  الأعداد** (v1.14.0 · قرار أحمد). عنوان الصفحة بيقول إن دي شاشة التغليف،
  والتقسيمة اللي الموظف **بيتصرّف عليها** هي مربعات المندوب. 🔴 **«محاولة
  مكرّرة» فضلت بقرار** — دي مش تقسيمة، دي إشارة إن الأوردر بيتشحن تاني.
  و`rdySetCounts` بقت `(total, repeat)`: سيب الباراميترين الميّتين كان معناه
  إن قارئ الكود يفتكر إن فيه عنصرين بيتكتب فيهم وهما مش في الـ HTML أصلاً.
- 🔴 **`rdyDayDiff`/`rdyDayLevel`/`rdyOrderAge` بقوا تفويض للـ shell**
  (`wocDayDiff`/`wocDayLevel`/`wocOrderAge` · v1.14.0) — `print.html` بقت
  بتعرض نفس بادج عمر الأوردر، ونسختين معناهم إن نفس الأوردر يبان «منذ
  يومين» في صفحة و«منذ ٣ أيام» في التانية (درس R1). و`rdyCairoDayIndex`
  اتشال — كان نسخة ميّتة من `cairoDayIndex` بتاعة الـ shell.
- **الاسم المعروض بقى «قسم التغليف»** (v1.2.0) — **عرض بحت**. قيمة `tool`
  في D1 لسه `pack_checker`، والـ Worker ما اتلمسش، والأداة المستقلة
  `Orders-Packing-Checker` **محتفظة باسمها القديم** بقرار — عشان تفضل
  مميّزة عن الهب طول ما هي شغّالة بالتوازي كنقطة رجوع.

### `remove.html` (الحذف)

الأداة دي **هدّامة** — بتحذف بند من أوردر حي. قربها بضغطة من جدول التغليف
معناه إنها هتتستخدم أكتر، يعني حراسها بقت **أهم** مش أقل:

- نافذة التأكيد **بالتأكيدين الإلزاميين** (مراجعة المخزون + قطع الفاتورة).
- شرط `displayFinancialStatus = PENDING` (COD غير مسدد).
- `restock: false` **مفروضة من الواجهة والـ Worker** — دفاع مزدوج.
- الرجوع للوضع الافتراضي بعد أي حذف ناجح.
- انتقال `custom.manual_status → Pending Edit` بشرطه، و`cancel_manual_reason →
  المنتج غير متوفر`.
- 🔴 **القيمة كانت `عطلان` لحد v1.8.0 — وهي مش موجودة في `choices` أصلاً.**
  يعني **كل** حذف من أول يوم كان بيشيل البند وبيفشل في كتابة الحالة/السبب
  (`Value does not exist in provided choices`)، والموظف بيشوف توست أصفر بيختفي
  لوحده. القيمة الجديدة اتأكّد وجودها في القايمة الحيّة (07-09-2026).
- 🔴 **حارس §PREFLIGHT في الـ Worker بيمنع العملية بالكامل** — التعريف الحيّ
  بيتقرا **قبل** أي كتابة، وأي قيمة مرفوضة = `409` و**صفر لمسة على الأوردر**.
  التفاصيل الكاملة في `Order-Item-Remover/CLAUDE.md` §«حارس §PREFLIGHT».
- 🔴 **التحذير والفشل بقوا نافذة مش توست** (`§OUTCOME-MODAL` · قرار أحمد
  07-09-2026). التوست بيختفي لوحده، والحالتين دول بيسيبوا الأوردر في وضع
  محتاج تدخّل يدوي. النافذة بتعرض القيمة المرفوضة و**قايمة الخيارات المسموحة**
  والمطلوب عمله. **النجاح الكامل بيفضل توست** — مالوش أي فعل مطلوب.
  ⚠️ مفيش `onclick` على الـ overlay بتاعها — الضغط بره **مايقفلهاش**، عكس
  نافذة التأكيد. الموظف بيدوس على الشاشة كتير.
  ⚠️ **الكود مكرّر حرفيًا في `Order-Item-Remover/index.html`** — أي تعديل في
  الاتنين في نفس التمريرة (درس R1). الفرق المقصود الوحيد: الهب بيستخدم `esc`
  من الـ shell، والأداة المستقلة عندها `ocEsc` محلية.
- ⚠️ **الفتح التلقائي بيوصل لقايمة المنتجات بس** — مش بيحذف ومش بيختار بند.
- 🔴 **شريط التابات كان ناقص من الـ HTML لحد v1.5.1 — والسجل كان مستحيل يتفتح.**
  البانل `#logTabContent` و`switchMainTab` وكل `§LOG-JS` (الفلاتر · الجدول ·
  التصفيح · تصدير XLSX) و`ExcelJS` وCSS السجل كانوا **موجودين بالكامل**، بس
  كتلة `.main-tabs-bar` بزراريها (`mainTabRemove`/`mainTabLog`) مش موجودة —
  فمحدش بينادي `switchMainTab` أصلاً، والبانل عليه `display:none`. **صفر خطأ
  في الكونسول**: الصفحة بتفتح وتبحث وتحذف عادي، والناقص الوحيد تاب مالوش أثر
  بصري. نفس عيلة باج chip التاب في `print.html` — كتلة كانت تحت الهيدر القديم
  وراحت معاه وقت التحويل لـ `wocHeader`، بس هنا ما اتمسكتش لأن الصفحة تابها
  التاني هو السجل بس.
- ⚠️ **فلتر الموظف في السجل بيتعبّى من `get_employees` عند أول فتح للتاب**
  (`loadLogEmployees`). في الأداة المستقلة ده كان **أثر جانبي** لـ
  `loadEmployeesList()` بتاعة شاشة الدخول، وشاشة الدخول اتشالت من الصفحة دي
  (`§AUTH-JS` — الدخول في `index.html`). من غير النداء ده الفلتر بيفضل
  «كل الموظفين» بس **من غير أي خطأ**. والفشل **مايوقّفش** السجل — الجدول
  بيتجاب عادي والفلتر بيفضل بخيار واحد.
- ⚠️ **`journey.html` مش بديل لسجل الحذف** — هي بتدوّر برقم أوردر واحد عبر
  التلات أدوات؛ السجل ده فيه فلتر موظف + بحث نصّي + تصفيح + تصدير XLSX للكل.

### `journey.html` (رحلة الأوردر)

- **`allSettled` مش `all`** — أداة واقعة مالهاش تخفي تاريخ الأداتين التانيين،
  والمصدر الفاشل بيتعرض **بالاسم**.
- **`search` بيقارن `order_name LIKE %x%`** — `#5362` بيطابق `#53621` كمان،
  فالفلترة **بالمطابقة التامة** في الواجهة ورقم الأوردر بيتعرض في كل صف.
- **`getLogs` بيستبعد `login`/`logout` في SQL** — وده صح هنا، إحنا عايزين الأفعال.
- **سقف ١٠٠ صف لكل أداة**، وبانر لو مصدر رجّع السقف.
- **صفوف `metafields_change` مش معروضة** — محتاجة مصدر رابع.

### `stats.html` (إحصائيات المخزن — **جديدة v1.5.0**)

الصفحة **قراءة D1 بحتة**: صفر نداء شوبيفاي · صفر كتابة · صفر `type` جديد ·
صفر تعديل Worker · صفر ترفيع لأي `min`. التفاصيل الكاملة والقرارات في
`docs/STATS-PAGE-PLAN.md`.

- 🔴 **يوم القاهرة مش يوم UTC.** `buildLogFilterSQL` بيفلتر بـ
  `substr(timestamp,1,10)` = **UTC**، ويوم القاهرة D بيبدأ `D-1T21:00Z`.
  فالصفحة بتطلب **يوم UTC زيادة** (`dateFrom = prevStart − 1`) وبتفلتر
  بنفسها بـ `cairoDayStr`. **شيل السطر ده = «إحصائيات النهاردة» بتفقد من
  ٩ مساءً لـ ١٢ بالليل من غير أي خطأ.**
- 🔴 **`extra.result` ليها تلات أشكال** — `stResultStatus(toolKey, ex)` هي
  المصدر الوحيد لقراءتها:

  | الأداة | الشكل |
  |---|---|
  | الطباعة | **كائن** `{ status, actions, warnings, errors, statusBefore }` |
  | التغليف · الحذف | **نص** `'success'` / `'warning'` |

  قارئ موحّد (`ex.result === 'warning'`) بيرجّع `false` **دايمًا** على صفوف
  الطباعة، ومؤشر تحذيرها بيفضل **صفر للأبد** من غير أي خطأ. والصف اللي مالوش
  الحقل بيرجّع `null` **مش `'success'`** — الصفوف القديمة إحنا مش عارفينها.
- 🔴 **الرقم العابر للأدوات بيتقفل «تعذّر» — مابيقلّش.** «موظفين عملوا
  عمليات» و«فرق الفترة» والوسيط كلهم بيتعرضوا `تعذّر` لو أي مصدر واقع، بينما
  أرقام الأدوات الناجحة بتفضل معروضة. إجمالي ناقص بيرندر زي السليم بالظبط،
  وده أخطر من شاشة فاضية (`ecommoda-dashboard-builder` Step 6 + قاعدة ٤).
- 🔴 **«موظفين عملوا عمليات» مش «الحضور»** — الليبل ده **قرار**.
  `login`/`logout` مستبعدين في **SQL** جوّه التلات Workers فمستحيل يوصلوا.
  لو الاسم بقى «الحضور»، الصفر بيتقري «محدش جه» بينما الحقيقة «محدش عمل حاجة».
- 🔴 **«فرق الفترة» (طبعنا − غلّفنا) محايد — ممنوع يبقى قايمة أخطاء.** أوردر
  اتطبع ٦ مساءً وهيتغلّف بكرة الصبح **مسار طبيعي**. السؤال الصح مكانه طابور
  التغليف (`isPrintedNotPacked`). أي شذوذ حقيقي مكانه `docs/ALERTS-TOOL-PLAN.md`
  عشان هناك القواعد ليها شرط إقفال تلقائي.
- ⚠️ **الوسيط بيقرا من مصدر رابع** — `POST /logs` بتاع الطابعة (٥٠٠٠ صف في
  نداء واحد، **بلا `extra`**) بنافذة ٧ أيام إضافية. من غيرها الأوردرات
  البطيئة (اللي اتطبعت قبل الفترة) بتتستبعد بالتعريف والرقم يبقى **متحيّز
  لأسفل** وبيتحسّن كل ما الشغل يسوء. والوسيط **مايتعرضش** تحت `ن = 5`.
- ⚠️ **آخر طباعة قبل التغليف بس** — طباعة بعد التغليف مش جزء من الدورة
  (اتأكّد في اختبار المتصفح: أوردر اتغلّف ١١ ص واتطبع ١٠:٣٠ م اتستبعد صح).
- ⚠️ **`STATS_MAX_PAGES = 15` حارس حلقة مش سقف بيانات** — لو اتضرب، **بانر
  اقتطاع أصفر إلزامي**. والاقتطاع بانر **منفصل** عن بانر الفشل (نفس تفرقة
  `s2Failed` / `s2Truncated`).
- ⚠️ **مفيش polling هنا، وده مش تناقض مع الشاشة الرئيسية.** الرئيسية طابور
  شغل **حي** الموظف بيتصرّف عليه دلوقتي؛ دي **مرجع بيتقرا**. مصدرين للتحديث
  بس: الزرار وتغيير الفترة (`ecommoda-dashboard-builder` Step 2 قاعدة ١).
  النبضة (٣٠ ثانية) عرض بحت **بلا أي نداء شبكة**.
- ⚠️ **الختم مابيتحرّكش لو كل المصادر فشلت** — ما نقولش للموظف إن الأرقام
  دي جديدة وهي مش جديدة (قاعدة ٥).
- ⚠️ **صف «غير مسجّل» في جدول الموظفين مايتحذفش** — الطابعة عمود `employee`
  فاضي في كل صفوفها قبل v2.0.0. حذف الصف بيخلّي مجموع الجدول ≠ المربعات فوق.
- ⚠️ **`.st-hours` عليها `direction:ltr`** — من غيرها الصفحة RTL بتقلب المحور
  فيبدأ من ٢٣ وينتهي عند ٠٠. اتمسكت في **اختبار متصفح فعلي**، مش في مراجعة.
- ⚠️ **الرسم CSS خالص — مفيش مكتبة رسم.** ٢٤ عمود مكدّس بـ `height` نسبي.
  انحراف مقصود عن الخطة (اللي كانت بتقول Chart.js): CDN محجوب = قسم مكسور
  **في صمت**، والرسم ده أبسط من إن التكلفة دي تستاهل. ومعاه **جدول مكافئ**
  في `<details>` (Step 7).
- ⚠️ **`MIN_DELTA_BASE = 5`** — تحت كده اعرض الانتقال (`0 ← 1`) مش نسبة.
  `prev = 1` بتطلّع «▲ 4000%»، رقم بلا معنى بياكل مصداقية الشاشة كلها.

### `sku-barcode.html` (باركود SKU — **دفعة أوردرات + سجل من v1.15.0**)

الصفحة **صفر كتابة على شوبيفاي**، بس **بقت بتكتب في D1** من v1.15.0
(Worker v1.2.0): صف سجل لكل عملية طباعة تحت
`order_sku_barcode_printer`/`print`·`print_sku`. الـ Worker في ريبو
`Order-SKU-Barcode-Printer` والتفاصيل الكاملة والانحراف الباقي في
`CLAUDE.md` بتاعه.

**التغيير الكبير في v1.10.0 — من أوردر واحد لدفعة:**

| | لحد v1.9.0 | من v1.10.0 |
|---|---|---|
| المداخل | مربعان: أوردر · **SKU/باركود** | **تلاتة**: شوبيفاي · بوسطة · يدوي — **كلهم أوردر** |
| النطاق | أوردر **واحد**، وأي بحث جديد بيمسح اللي قبله | **دفعة**: أوردر ورا التاني بيتراكموا |
| الطباعة | ليبلات أوردر واحد | **الدفعة كلها في ملف واحد**، مرتّبة أوردر ورا أوردر |

**والتمريرة التانية في v1.15.0 (قرار أحمد 08-09-2026):**

| | لحد v1.13.0 | من v1.15.0 |
|---|---|---|
| المداخل | تلاتة — كلهم **أوردر** | **أربعة**: التلاتة + مربع **SKU مباشر بدون أوردر** |
| السجل | ❌ مفيش — «مفيش أثر لمين طبع إيه» | ✅ **تاب سجل عمليات** (فلاتر · تصفيح · XLSX) |
| الليبل | SKU + باركود + **رقم الأوردر** 8px · خط سيريف | **SKU + باركود بس** · خط grotesque محايد |
| شريط الدفعة | سطر نص + شيلة لكل أوردر + «مسح الدفعة» | **بادجان كبار** + زرار الطباعة · **مسح تلقائي بعد الطباعة** |
| الجدول | ٦ أعمدة (صورة · المنتج · SKU · Barcode · كمية · طباعة) | **٤ أعمدة** (صورة · SKU · كمية · طباعة + ✕) |
| الحذف | «✕ شيل من الدفعة» على **الأوردر كله** | **✕ لكل صنف** جنب خانة عدد الطباعة |
| الصورة | مصغّرة 40px وبس | **الضغط بيفتحها بالحجم الكامل** |
| أزرار الدفعة | «نسخة لكل صنف» · «بالكمية» · «مسح الدفعة» | **التلاتة اتشالوا** |
| البحث بـ SKU | ❌ اتشال في v1.10.0 | ✅ **رجع** — كمدخل رابع بقايمة اختيار |

**والتمريرة التالتة في v1.16.0 (قرار أحمد 08-09-2026):**

| | لحد v1.15.0 | من v1.16.0 |
|---|---|---|
| البحث بالـ SKU الكامل | ❌ **مكسور** — بيرجّع «مفيش أي صنف» على صنف موجود | ✅ شغّال (Worker `1.2.1`) |
| قايمة الاختيار | **مودال** بعد ضغطة «بحث» | **قايمة منسدلة بتفتح وانت بتكتب** (§SKU-AC) |
| زرار ✕ | **بيحذف الصنف من العرض** | **بيشطبه ويصفّر عدده وبيسيبه معروض** — والضغطة التانية بترجّعه |
| مربع الـ SKU | عمود رابع في نفس الصف | **صف لوحده بعرض الصفحة** تحت التلاتة |
| المربعات | `placeholder` + سطر شرح تحت كل واحد | **بلا الاتنين** — العنوان واللون هما التعريف |
| الجدول | هيدر وقيم على اليمين | **كل خلية متوسّطة + خطوط رأسية** (المعيار الموحّد) |
| صف المجموعة | رقم الأوردر + «N صنف» + بادج تراكينج | **رقم الأوردر وبس** |
| التسمية | «ليبل» | **«باركود-SKU»** في كل الواجهة |

- 🔴 **الباج اللي التمريرة اتعملت عشانه: البحث بالـ SKU الكامل كان بيرجّع
  صفر على صنف موجود.** الـ Worker كان بيبعت المدخل لشوبيفاي زي ما هو
  (`(sku:${term}*) OR (barcode:${term})`)، وبحث شوبيفاي بيقسّم
  `SD1 / Light grey / 45` لكلمات منفصلة — فـ `Light` و`grey` بقوا شروط بحث
  عامة والنتيجة **صفر**. الرد `200` بمصفوفة فاضية، والواجهة بتقول «مفيش أي
  صنف بالـ SKU ده على شوبيفاي» على صنف **الموظف شايفه في الجدول فوق**.
  الإصلاح في `Order-SKU-Barcode-Printer` §`buildSkuQuery` (Worker `1.2.1`)،
  واتأكّد **حيًا على المتجر** 08-09-2026 على `SD1` و`MRX155` و`RN-AD`.
  ⚠️ **و`sku:"..."` (اقتباس) مش الحل** — بيشتغل للتطابق التام بس، و
  `sku:"SD1 / Light"` بيرجّع صفر، يعني كان هيكسر البحث الجزئي اللي القايمة
  المنسدلة قايمة عليه.
- 🔴 **§SKU-AC — القايمة بتفتح وانت بتكتب، ومسار الاختيار واحد.** زرار
  «بحث» والكتابة المباشرة **الاتنين بيعدّوا على نفس `bcSearchSku` ونفس
  `bcAcOpen`**؛ المودال اتشال بالكامل. مسارا اختيار = يفترقوا مع أول تعديل
  (درس R1).
  ⚠️ **`bcAcSeq` حارس تزامن مش تحسين** — الموظف بيكتب أسرع من الرد، وردّ
  حرف قديم بيوصل بعد الجديد فبيدهس القايمة بنتايج **نص الكلمة اللي فاتت**
  من غير أي خطأ.
  ⚠️ **مهلة ٣٥٠ms مش ضبط أداء** — من غيرها كل حرف بيطلّق نداء GraphQL كامل
  (ونداء OAuth وراه). و**أقل من ٣ حروف مفيش قايمة** — حرفان بيطابقوا نص
  الكتالوج.
  ⚠️ **القفل على `mousedown` بره الكارت مش على `blur`** — الضغط على صف في
  القايمة بيسحب الفوكس، و`blur` كان بيقفلها **قبل** ما `click` يوصل للصف،
  يعني الاختيار بالماوس مايشتغلش خالص.
  ⚠️ **فشل الاقتراح مابيدخلش بانر الفشل** (`#bcFails`) — الموظف لسه بيكتب،
  والبانر ده موجود عشان **أوردر ضاع من دفعة**. ⛔ ممنوع يتلوّث بسطر أحمر
  على كل كلمة نص مكتوبة.
- 🔴 **✕ بقى «استبعاد» مش «حذف» — والصف بيفضل معروض مشطوب.** الحذف من
  العرض كان بيخلّي الاستبعاد **غير مرئي**: الموظف بيبص على الجدول
  ومايعرفش هو استبعد الصنف ده ولا الجلب أصلاً ما رجّعوش — والفرق بين
  الاتنين هو الفرق بين «مش عايزه» و«قطعة هتخرج من غير باركود». نفس مبدأ
  «صف بلا قناة بيظهر ومربعه مقفول» في `print.html`.
  ⚠️ **الضغطة التانية بترجّعه بعدده اللي كان** (`copiesBefore`) — مش بكمية
  البند: الموظف ممكن يكون عدّل الخانة بإيده قبل الاستبعاد، والرجوع للكمية
  كان هيدهس التعديل ده في صمت.
  ⚠️ **والمجموعة مابتتشالش أبدًا دلوقتي** — البند القديم «آخر صنف بيشيل
  مجموعته معاه» **اتلغى** لأن مافيش صف بيتشال أصلاً. البادجات لسه بيعدّوا
  المعروض في الجدول بالحرف.
- 🔴 **الجدول بقى على المعيار الموحّد** (`ecommoda-html-builder` §Data
  Tables): كل `th` و`td` متوسّطة + خطوط رأسية خفيفة بـ `border-inline-start`.
  `.data-table` بتاع تاب السجل كان مظبوط أصلاً، و`.bc-table` كان الوحيد
  اللي برّه المعيار. ⚠️ **`direction:ltr` فضل** على الـ SKU والأرقام —
  اللي اتغيّر هو `text-align` بس.
- 🔴 **صف المجموعة بقى رقم الأوردر وبس.** بادج التراكينج **معلومة إدخال**
  (الموظف سكنها بإيده وشافها بتتحوّل)، وعدّاد «N صنف» بيتقري بالعين من
  الصفوف تحته — الاتنين كانوا بيزاحموا الرقم اللي الصف كله موجود عشانه.
- ⚠️ **مفيش `placeholder` ولا `.scan-hint` في أي مربع.** النص الرمادي جوّه
  المربع بيتقري **قيمة مكتوبة** من بعيد على شاشة المحطة. و**`aria-label`
  فضل على كل مربع** — هو الوصف الوحيد الباقي لقارئ الشاشة.
  ⚠️ **بس قاعدة `.scan-hint` فضلت في الـ CSS** رغم إنها بلا مستهلك هنا —
  الكتلة دي **نسخة طبق الأصل من `pack.html`**، وشيلها من ناحية واحدة بس
  كان بيكسر التطابق اللي القاعدة مبنية عليه.
  ✅ و`.entry-grid-4` **اتشالت**: الشبكة رجعت `.entry-grid` بتلات أعمدة
  بالحرف زي `pack.html`، واللي زايد هنا سطر واحد (`grid-column:1/-1` على
  كارت الـ SKU).

**والتمريرة الرابعة في v1.17.0 (قرار أحمد 08-09-2026):**

| | لحد v1.16.0 | من v1.17.0 |
|---|---|---|
| صف نتيجة البحث | SKU + **اسم المنتج + الباركود** | **الـ SKU وبس** بخط ١٥px |
| الاختيار من القايمة | صنف واحد بالضغط | **تحديد متعدد** + «تحديد الكل» + «إضافة المحدد (N)» |
| كمية الصنف بدون أوردر | `1` ثابتة | **المتاح على شوبيفاي** (`available`) — وعدد الطباعة بيتبعها |
| الـ SKU | نص | **لينك لصفحة المتغيّر في الأدمن** (الجدول والقايمة) |
| نص الانتظار | «بيدوّر على…» | **«جاري البحث عن…»** |
| سجل التغييرات | لسه فيه «ليبل» | **«باركود-SKU»/«ملصق»** زي باقي الواجهة |

- 🔴 **التحديد المتعدد — والسبب مش راحة.** البحث الجزئي (`RN-AD-35`) بيرجّع
  **كل** الألوان والمقاسات، والحالة الواقعية إن الموظف عايز منهم كذا واحد.
  الإضافة الفورية كانت بتقفل القايمة بعد كل صنف، فبيعيد كتابة نفس الكلمة من
  الأول لكل مقاس — في دفعة ٦ مقاسات دي **٦ عمليات بحث كاملة على شوبيفاي**.
  ⚠️ **التطابق التام لسه بيتضاف على طول من غير قايمة** — اللي كاتب الـ SKU
  كامل مايستناش خطوتين.
  🔴 **الصف بقى `label` جوّاه `checkbox`** — الضغط على أي مكان فيه بيعلّم
  المربع بلا أي JS، وقارئ الشاشة بيقرا الـ SKU كاسم للمربع. زرار جوّه
  `label` = ضغطة بتعمل حاجتين.
  🔴 **«تحديد الكل» مابيحددش الصنف اللي مالوش باركود** — `bcAddSkuVariant`
  بترفضه أصلاً، فمربع مفتوح عليه كان هيخلّي «إضافة ٣» تضيف **اتنين في صمت**.
  وفيه بند في فحص المتصفح بيقفل ده.
  ⚠️ **الإضافة بتعدّي على `bcAddSkuVariant` صنف صنف** — مش مسار جديد. هي
  اللي فيها حارس التكرار وحارس الباركود وعدّاد النسخ (درس R1). والتوست
  **مرة واحدة للدفعة** — ٦ توستات ورا بعض بتغطّي الشاشة.
- 🔴 **صف النتيجة بقى الـ SKU وبس.** اسم المنتج سطر طويل بيلفّ فبيقلّل عدد
  النتايج الظاهرة، ورقم الباركود **محدش بيقارنه على الشاشة** (بيتقري من
  الملصق المطبوع). اللي بيطابق القطعة اللي في إيد الموظف هو **الـ SKU
  والصورة**. ⚠️ **بس «مفيش Barcode» فضل** — ده اللي بيقول إن الصنف **مش
  هيتطبع خالص**، ومربع اختياره مقفول عشان كده.
- 🔴 **كمية الصنف بدون أوردر = المتاح على شوبيفاي، وعدد الطباعة بيتبعها.**
  ده **نفس** قاعدة صفوف الأوردر بالحرف («عدد النسخ الافتراضي = كمية
  البند»). قاعدة تانية هنا كانت هتخلّي صف يقول «الكمية ٤ · الطباعة ١» جنب
  صف أوردر بيقول «٢ · ٢»، والموظف يقراها **باج مش قرار**.
  🔴 **الاستثناء الوحيد: صفر أو غير معروف → عدد الطباعة `1`.** الحالة اللي
  المدخل ده اتعمل عشانها أصلاً هي **باركود-SKU ضاع من على قطعة راجعة**،
  ومخزونها بيبقى صفر غالبًا. صفر في خانة الطباعة معناه إن الصف **مش هيتطبع
  خالص** — الموظف يدوّر ويلاقي ويطلع بورق فاضي. وعمود «الكمية» بيفضل بيقول
  **الرقم الحقيقي** (صفر أو `—`)، فمفيش رقم بيكدب.
  ⚠️ **`—` مش `0` لما الرقم مش معروف** — الـ Worker بيرجّع `null` لو
  `read_inventory` ناقصة أو التتبّع مقفول على المتغيّر، و«صفر» ساعتها
  بيتقري «مفيش في المخزن» بدل «الرقم مش معروف».
- 🔴 **`shopifyVariantUrl` في `shared/shell.js` §HELPERS — مش في الصفحة.**
  الصفحة بتستخدمها في **الجدول وفي القايمة**، وأي صفحة تانية هتعرض SKU
  هتحتاجها. ⚠️ والرابط **مركّب من `productId` و`variantId` مع بعض** —
  الدالة بترجّع `null` لو أي واحد ناقص والمستدعي بيعرض **نص عادي**: لينك
  رايح لـ `products/undefined` بيفتح صفحة خطأ والموظف يفتكر إن الصنف اتمسح
  (نفس منطق `orderLink` بالحرف).

**والتمريرة الخامسة في v1.19.0 (قرار أحمد 09-09-2026):**

| | لحد v1.18.0 | من v1.19.0 |
|---|---|---|
| ترتيب المجموعات | **الأقدم فوق** (ترتيب الإضافة) · ومجموعة «بدون أوردر» آخر واحدة دايمًا | **الأحدث فوق** — آخر إدخال أول مجموعة، والقاعدة واحدة على الكل |
| شريط «تحديد الكل» و«إضافة المحدد» | **آخر** القايمة المنسدلة (`sticky bottom`) | **فوق النتايج** ملزوق تحت المربع (`sticky top`) |
| الضغط على ✕ | بيرجّع الفوكس لمربع الإدخال — **الشاشة بتترمي لفوق** | **مافيش نقل فوكس** — الجدول بيفضل مكانه |

#### 🔴 §NEWEST-FIRST — آخر إدخال فوق (v1.19.0 · قرار أحمد 09-09-2026)

الجدول كان بترتيب الإضافة، فالأوردر اللي الموظف **لسه سكنه** بيروح آخر
الجدول: بعد رابع أوردر لازم ينزّل عشان يتأكد إنه اتضاف أصلاً، والأصناف اللي
بيراجع عددها هي **دايمًا** بتاعة آخر سكانة.

- 🔴 **النقل بيحصل في `bcOrders` نفسه — مش في الرسم** (`bcOrders.unshift`
  للأوردر الجديد · `bcBumpToTop` لمجموعة «بدون أوردر» · `rows.unshift`
  للصنف الجديد). المصفوفة دي هي ترتيب العرض **وترتيب الطباعة** مع بعض
  (§PRINT-JS)، وترتيب الطباعة بقى **الرابط الوحيد** بين الليبل وأوردره من
  v1.15.0 (§LABEL-2LINE). ترتيب مقلوب في الرسم لوحده معناه إن الورق يطلع
  بترتيب **غير** اللي على الشاشة — أسوأ بمراحل من الترتيب القديم.
  ⛔ ممنوع ترتيب محلي في `bcRenderBody` ولا في `bcPrint`، وفيه بند في فحص
  المتصفح بيقفل تطابق الاتنين.
- 🔴 **مجموعة «بدون أوردر» بقت بتتحرّك زي أي مجموعة** — البند القديم
  («مكانها آخر الكومة دايمًا») **اتلغى** بقرار: القاعدة بقت واحدة، واستثناء
  لمجموعة واحدة كان هيخلّي صنف اتضاف **دلوقتي** يقع تحت أوردر اتسكن من ٥
  دقايق. وإضافة صنف جديد بتطلّع المجموعة فوق كمان.
- ⚠️ **دفعة «إضافة المحدد» بتتضاف بالعكس عن قصد** — كل صنف بيتحط فوق، فالمرور
  على المحدَّدين من الآخر للأول هو اللي بيخلّي الدفعة تظهر **بنفس ترتيب
  القايمة** اللي الموظف حدّد منها. من غيره الأربعة بيطلعوا مقلوبين.
- ⚠️ **البادجان ما اتغيّروش** — لسه بيعدّوا المعروض في الجدول بالحرف، والترتيب
  مالوش أي أثر على العدّ ولا على حارس التكرار (`bcFindOrder` بيدوّر بالمفتاح).

#### 🔴 §SKU-BAR-TOP — شريط التحديد فوق النتايج (v1.19.0)

«تحديد الكل» و«إضافة المحدد (N)» كانوا في **آخر** قايمة ممكن تبقى ٥٠ صف،
فالموظف بيحدّد وهو فوق وينزل يدوّر على الزرار. دلوقتي الشريط **أول عنصر في
القايمة** و`sticky` من فوق — في نفس مستوى المربع اللي لسه بيكتب فيه.

- ⚠️ **`sticky` لسه شرط** — بس من فوق (`top:-6px`). من غيرها الشريط بيروح مع
  التمرير في النتايج، وهو بالظبط اللي القرار اتعمل عشان يمنعه.
- 🔴 **ومربع «تحديد الكل» بقى أول `.bc-pick-cb` في الـ DOM** — أي محدِّد
  بيقول `.bc-pick-cb` وبعدها `.first()` بيمسكه هو مش أول صف نتيجة. فحص
  المتصفح بيسمّي `.bc-pick-row` صراحةً عشان كده (البند كان بيعدّي **غلط**).

#### 🔴 §ROW-X-NO-FOCUS — ✕ مابيحركش الشاشة (v1.19.0)

`bcToggleRow` كانت بتنادي `focusEntryInput()` بعد الرسم، والمداخل **فوق
الجدول** — يعني كل استبعاد كان بيرمي الصفحة لفوق والصف اللي لسه اتشطب يختفي
من قدام الموظف. والموظف بيستبعد كذا صنف ورا بعض، فالقفزة بتتكرر مع كل ضغطة.

- ⚠️ **مالوش أثر على السكانر** — الضغطة اللي بتسحب الفوكس من الحقل هي ضغطة
  الماوس على الزرار نفسه (سلوك المتصفح)، والموظف اللي بيسكن مش ماسك الماوس
  أصلاً. الاستدعاء المتشال كان **بيرجّع** الفوكس بعد ما الماوس سحبه، بتكلفة
  قفزة في الشاشة.
- ⚠️ **`bcClearAll` لسه بتنقل الفوكس** — دي بتمسح الدفعة كلها بعد الطباعة
  والشاشة بتفضى فعلاً، فالرجوع للمربع هو المطلوب مش قفزة.

- 🔴 **`bcAddOrder` هي المسار الوحيد لإضافة أوردر.** التلات مداخل الأولانيين
  بيعدّوا منها، والفرق الوحيد بينهم هو إزاي الرقم بيتحوّل لاسم أوردر **قبل**
  النداء. مسار إضافة تاني = حارسين تكرار يفترقوا (درس R1).
  ⚠️ **مدخل الـ SKU مسار منفصل (`bcAddSkuTerm`) — وده مش خرق للقاعدة:** هو
  بيضيف **صنف** مش أوردر، وبينادي `search_sku` (`productVariants`) مش
  `get_order`. و`sku:`/`barcode:` **مش صالحين على `orders`** أصلاً —
  بيرجّعوا صفر نتايج **بلا أي خطأ** (`shopify-graphql-helper` Step 3)، يعني
  «توحيد» المسارين كان بينتج بحث بيرجّع فاضي في صمت.
- 🔴 **`bcOrders` هو مصدر الرسم والطباعة الوحيد** — ومجموعة «بدون أوردر»
  عايشة **جوّاه** كمجموعة بمفتاح محجوز (`BC_SKU_KEY = '__sku__'`، `name`
  بتاعها `null`)، مش في مصفوفة تانية. مصفوفة تانية كانت هتطلّب فرع
  `if (sku)` في الرسم والطباعة والحذف والعدّ — أربع فروع تفترق مع أول
  تعديل (درس R1).
- 🔴 **`bcTotalOrders()` بتستبعد مجموعة «بدون أوردر»** من بادج «أوردر» —
  عدّها كأوردر معناه بادج بيقول ٣ وجدول فيه أوردرين. **البادجان بيعدّوا
  اللي معروض في الجدول تحتهم بالحرف** (نفس درس «بادج ٦٦ فوق جدول فيه ٦»
  في أداة الطباعة · v1.11.1)، وفيه بنود في فحص المتصفح بتقفل ده.
- 🔴 **تراكينج بوسطة بيتحوّل في Worker التغليف** — فوق في `pack.min`.
  ومنه كمان `get_employees` لفلتر السجل (v1.15.0).
- 🔴 **الأوردر الفاشل بيفضل معروض باسمه** — اتنقل من شيلة لـ **بانر أحمر**
  (`#bcFails`) في v1.15.0 لما الشيلات اتشالت. **اللي اتشال شكل الشيلة، مش
  الإعلان عن الفشل**: دفعة ٨ سكانات واحد فيها وقع والموظف طبع من غير ما
  يعرف = قطعة بتخرج من المخزن من غير ليبل. ⛔ ممنوع يتحوّل لتوست بيختفي
  لوحده. ونفس المبدأ في القطع (`itemsTruncated`): البانر **بيسمّي
  الأوردرات المقطوعة بالاسم**.

#### 🔴 §LABEL-2LINE — الليبل بقى سطرين (v1.15.0 · قرار أحمد 08-09-2026)

سطر رقم الأوردر تحت الخطوط **اتشال**، وخط الليبل اتغيّر من سيريف (Georgia)
لـ grotesque محايد (`Arial, 'Helvetica Neue', Helvetica, 'Liberation Sans', Arimo`).

- 🔴 **الثمن اللي لازم يتقال:** السطر ده اتضاف أصلاً في v1.10.0 لأن الورق
  بيطلع من الطابعة **مكوّم**. من غيره **الرابط الوحيد بين الليبل وأوردره
  بقى ترتيب الخروج من الطابعة** — وده لسه **مجموعة ورا مجموعة** زي الشاشة
  بالحرف (§PRINT-JS)، وفيه بند في فحص المتصفح بيقفل الترتيب ده.
  ⛔ ترتيب الطباعة بقى **جزء من العقد**، مش تفصيلة تنفيذ.
- ⚠️ **ومن v1.15.0 فيه ليبلات من غير أوردر أصلاً** (مدخل الـ SKU) — يعني
  السطر ماكانش هيبقى موجود على كل ليبل حتى لو اتساب.
- ⚠️ **الخط ستاك نظام بقرار — مفيش خط من الشبكة.** خط من Google Fonts في
  ليبل مطبوع معناه: CDN محجوب → خط احتياطي **بعرض حروف مختلف** → و
  `bcFitLabelText` قاست على خط ما اتحمّلش → نص بيخرج بره الليبل **من غير
  أي خطأ**.
- 🔴 **الميزانية اتفضّت ١٢.٨px والباركود أخد منها** (٣٤ → ٤٠px). المحتوى
  المقيس فعليًا **٦٢.٨px** من ٧٦.٨px متاحين. القص لسه صامت
  (`overflow:hidden`) — بس دلوقتي بياكل من **خطوط الباركود نفسها** بدل ما
  ياكل سطر نص، يعني السكانر بيبطّل يقرا. **متزوّدش أي مقاس من غير ما تقرا
  الرقم من فحص المتصفح** (بند ⑪) — `JsBarcode` بترسم الـ SVG بارتفاع أقل
  من `height` اللي بتتبعت لها، فالحساب من الإعدادات بيغلط.

#### 🔴 §AUTO-CLEAR — الدفعة بتتمسح بعد الطباعة (v1.15.0)

«مسح الدفعة» اتشال بالكامل، والمسح بقى تلقائي على `afterprint`.

- 🔴 **المتصفح مابيقولش لو الموظف لغى الطباعة.** `afterprint` بيتطلّق في
  الحالتين — طبع فعلاً أو دوس Cancel — ومفيش أي إشارة تفرّق بينهم في أي
  متصفح. **يعني إلغاء الطباعة بيمسح الدفعة والموظف بيعيد السكان.** ده ثمن
  معروف ومقبول في القرار، واللي بيخفّفه حاجتان:
  ① **التوست بيقول إن المسح حصل وبكام ليبل** — مايبقاش «الشاشة فضيت لوحدها».
  ② **صف D1 بيتكتب قبل `window.print()`** — فالمسح مش بيضيّع أثر العملية.
- ⛔ **ممنوع `setTimeout` كبديل لـ `afterprint`** — المهلة الثابتة بتمسح
  الدفعة **والنافذة لسه مفتوحة قدام الموظف**.
- ⚠️ **الـ listener بيتربط مرة واحدة عند التحميل** و`bcPendingClear` هي
  اللي بتتحكم. ربطه جوّه `bcPrint` كان هيراكم listener على كل ضغطة، فدفعة
  تانية كانت هتمسح مرتين وتطلّع توستين.

#### 📜 §LOG — تاب سجل العمليات (v1.15.0 · Worker v1.2.0)

- 🔴 **الصف بيتكتب قبل `window.print()` مش بعدها** — نافذة الطباعة بتوقف
  التنفيذ، ولو الموظف قفل التاب وهي مفتوحة الصف مايتكتبش خالص.
- 🔴 **فشل التسجيل مايمنعش الطباعة أبدًا** — توست أصفر والورق بيطلع. حارس
  بيمنع الشغل بيتشال بعد أسبوع (نفس مبدأ «فشل جلب السجل مايمنعش الطباعة»
  في بوابة المراجعة بتاعة `print.html`).
- 🔴 **صف لكل أوردر — مش صف للدفعة ولا صف لكل ليبل.** صف واحد للدفعة =
  `order_name` فاضي والسجل مش قابل للبحث برقم أوردر (وده **أول** سؤال
  بيتسأل)؛ صف لكل ليبل = دفعة ٤٢ ليبل بتكتب ٤٢ صف. الأصناف في
  `extra.items`. **والأصناف بدون أوردر بتاخد صف لكل صنف** — هناك الصنف
  **هو** وحدة العملية.
- ⚠️ **فلتر الموظف من Worker التغليف** (`get_employees`) — Worker الباركود
  مالوش نقطة دخول بقرار. والفشل **مايوقّفش** السجل: الجدول بيتجاب عادي
  والفلتر بيفضل بخيار واحد.
- ⚠️ **`fetchLog`/`renderLogTable`/`renderPagination`/`goPage` في القايمة
  السودا** — بيفضلوا في الصفحة، ونسخة `remove.html` بتفضل هناك.
- ⚠️ **`logFilterParams()` مصدر واحد للفلاتر** — العرض والعدّ والتصدير
  التلاتة بيعدّوا منه. فلاتر مختلفة بين التصدير والعرض = ملف بينزّل غير
  اللي على الشاشة، ونسبة «٢٠٠٠ من ٥٠٠٠» كذّابة (عقد `get_logs_export`).
- ⚠️ **`type` هنا نص بسيط** (`print`/`print_sku`) — **مش** كائن زي
  `extra.result` بتاع أداة الطباعة (الفخ المكتوب في قسم `stats.html`).
  ومفيش `print_failed`: الفشل الوحيد الممكن (الطابعة) الأداة مش شايفاه.
- ⚠️ **فحص التكرار بيحصل مرتين** — على المدخل قبل النداء (بيوفّر استعلام
  كامل، وده أكتر حالة بتحصل: الموظف بيسكن نفس الملصق مرتين)، وعلى الاسم
  اللي رجع (لأن الـ ID مالوش اسم قبل الجلب).
- ⚠️ **`BC_ADD_CONCURRENCY = 3` مش ضبط أداء** — الإدخال اليدوي بيقبل لصق
  ١٥ رقم مرة واحدة، و`Promise.all` عليهم = ١٥ نداء متزامن كل واحد بيجرّ
  OPTIONS preflight ونداء OAuth على شوبيفاي، وده بالظبط اللي أنتج R15 في
  أداة الطباعة. والحلقة المحدودة بتخلّي **فشل أوردر واحد مايلغيش الدفعة**.

- 🔴 **الباركود بيشفّر `barcode` مش `sku`.** النص فوق الليبل هو الـ SKU
  (`FL-PO-10 / Black / 43`)، والرقم جوّه الخطوط هو الـ Barcode (`34271298`).
  عكسهم بيخلّي السكانر يقرا رقم **مالوش وجود** في المخزون — من غير أي خطأ.
- 🔴 **قياس عرض نص الـ SKU بـ Canvas `measureText` مش `el.scrollWidth`.**
  `#printArea` عليه `display:none` طول الوقت (بيبان في `@media print` بس)،
  فأي قياس معتمد على الـ layout بيرجّع **صفر دايمًا** وحلقة التصغير بتوهم
  إنها شغالة وهي واقفة من أول لفة. **متحوّلهاش لـ `scrollWidth`.**
- 🔴 **`requestAnimationFrame` مزدوجة قبل `window.print()`.** الـ SVG بيتضاف
  ديناميكيًا، والطباعة من غير انتظار رسم فعلي بتطلّع ورق **بلا باركود**.
  `setTimeout` بمدة ثابتة مش بديل — بيعدّي في الاختبار ويقع على جهاز أبطأ.
- 🔴 **المعاينة `cloneNode` من عناصر الطباعة نفسها** — مش بناء تاني. نسختين
  معناها إن اللي على الشاشة مش اللي هيطلع من الطابعة (درس R1).
- ⚠️ **JsBarcode من نفس CDN ونفس نسخة `print.html`** (`3.11.6` من jsdelivr).
  النموذج الأولي كان بيضمّن المكتبة حرفيًا؛ الضمّ اتشال عشان الهب مايبقاش
  فيه نسختين من نفس المكتبة. **ومقابل خطر الـ CDN المحجوب:** حارس
  `bcLibReady()` بيقفل زرار الطباعة برسالة صريحة — الشبكة المحجوبة بتبقى
  **فشل معلن** مش ليبل فاضي بيخرج من الطابعة.
- 🔴 **`.entry-grid` و`.entry-card` و`.scan-input` و`bindScannerInput`
  و`lastEntryMode` نسخة طبق الأصل من `pack.html`** — نفس أسماء الكلاسات
  ونفس القيم ونفس المهل (١٥٠ms للسكانة · ٤٠٠ms للكتابة اليدوي). الموظف
  بيتنقّل بين الصفحتين بنفس السكانة في إيده، وسلوك مختلف = «سكنت وماحصلش
  حاجة». **أي تعديل يتعمل في الصفحتين في نفس التمريرة** (درس R1).
  ⚠️ **الاستثناء الوحيد `.entry-grid-4`** (v1.15.0) — كلاس **إضافي** بيغيّر
  `grid-template-columns` لأربعة، لأن الصفحة دي بقى فيها مدخل رابع
  و`pack.html` لسه بتلاتة. الكلاس الأساسي و`.entry-card` و`.scan-input`
  **متطابقين حرفيًا** زي ما هم — عمود اتضاف، مافيش سلوك اتغيّر.
- ⚠️ **§SCAN على المربعين الأولانيين بس** (شوبيفاي · بوسطة). القراءة والمسح
  **جوّه `fire`** مش جوّه الـ listener — المسح في الـ listener بيقطع دفعة
  السكانر في نصّها.
  ⚠️ ومربع الـ SKU **مالوش §SCAN** زي اليدوي بالظبط: السكانة بتطلّق باركود
  أوردر أو ملصق بوسطة، مش نص SKU فيه مسافات وشرطات مايلة — وربط السكانر
  بيه كان هيطلّق بحث على نص نص وهو لسه بيتكتب.
  ⛔ **ومربع الـ SKU مابيتقسّمش على المسافات زي المربع اليدوي** — الـ SKU
  نفسه فيه مسافات (`RN-AD-115 / Black / 45`)، فالتقسيم كان هيحوّل مدخل
  واحد صحيح لتلات عمليات بحث فاشلة.
- ⚠️ **الإدخال اليدوي بيقبل أكتر من رقم** مفصولين بمسافة أو فاصلة (عربية أو
  إنجليزية) أو سطر جديد — الموظف عنده ورقة فيها أرقام الدفعة، فاللصق مرة
  واحدة أسرع من ١٥ ضغطة Enter. التكرار بيتشال **قبل** الجلب.
- ⚠️ **عدد النسخ الافتراضي = كمية البند** — ده أصل الأداة: كل قطعة بتخرج من
  المخزن ليها ليبل. **أما صنف من مدخل الـ SKU فبيبدأ من ١** — مفيش أوردر
  يقول كمية أصلاً، والحالة اللي المدخل ده اتعمل عشانها ليبل واحد ضاع.
  ⚠️ **زرارا «نسخة لكل صنف» و«بالكمية» اتشالوا في v1.15.0** — الافتراضي هو
  المستخدم فعليًا، والخانة بتصلّح صف بصف. زرار بيدهس **كل** الخانات في
  الدفعة كان أخطر من إنه مفيد: ضغطة واحدة بتلغي كل تعديل يدوي قبلها من
  غير أي تأكيد.
- ⚠️ **صنف بلا `barcode` مابيتطبعش** — خانة النسخ مقفولة على صفر، و«⚠️ مفيش
  Barcode» مكتوب **تحت الـ SKU**. عمود الـ Barcode نفسه اتشال في v1.15.0
  (الرقم بيتقري من الليبل ومحدش بيقارنه على الشاشة)، بس **غيابه فضل
  بيتقال** — ده اللي بيقول إن الصنف ده مش هيتطبع خالص. الحل على شوبيفاي
  مش هنا، والرسالة بتقول كده صراحةً.
- ⚠️ **زرار ✕ مش بديل لخانة الصفر — والعكس** (اتغيّر في v1.16.0): الصفر
  اليدوي معناه «عايز أعدّل العدد»، والشطب معناه «الصنف ده مستبعد من
  الدفعة». الاتنين بيوصّلوا لصفر نسخة، بس **الحالة المعروضة مختلفة**
  والرجوع من الشطب بضغطة. ⚠️ وبند «آخر صنف بيشيل المجموعة معاه» **اتلغى**
  — مافيش صف بيتشال أصلاً.
- ⚠️ **القطع بيتبلّغ عنه ببانر أصفر** (`itemsTruncated`) **بأسماء
  الأوردرات المقطوعة** — ليبل ناقص في السكوت = قطعة بتخرج من المخزن من غير
  باركود، و«فيه قطع» من غير أسماء يعني الموظف مايعرفش يراجع فين.
- 🔴 **الطباعة بتخفي باقي الصفحة بـ `display:none` — مش `visibility:hidden`.**
  التاني بيخفي العنصر و**بيسيبه واخد مكانه في التخطيط**، ومع
  `@page{size:2in 1in}` باقي الصفحة كان بيتمدّ ويتقسّم على **عشرات الصفحات
  البيضا** ورا الليبلات: **٥ ليبل = ملف ٣٧ صفحة، ٣٢ منهم بيضا**. ومعاه
  `min-height:100vh` بتاعة `body` في `shell.css` — **مساهم تاني**، بيفرض
  صفحة كاملة على الأقل حتى لو المحتوى أقصر. ⛔ ممنوع الرجوع لأي منهم.
  ⚠️ السببان كانوا **متراكبين** — إصلاح واحد لوحده ما كانش بيقفل البند،
  والتاني ما اتمسكش غير في **فحص المتصفح** بعد إصلاح الأول.
- 🔴 **ارتفاع الليبل مقفول على ١ إنش:** ٩٦px ناقص ١٩.٢px حشو = ٧٦.٨px
  متاح، والمحتوى **المقيس فعليًا** ٦٢.٨px (سطران + فراغ `.06in`).
  التفاصيل والخطر في §LABEL-2LINE فوق.
- ⚠️ **`--font-label` استثناء موثّق من Standards #11** — فوق في قاعدة الأسبقية.
  **وقيمته اتغيّرت في v1.15.0** (سيريف → grotesque) — §LABEL-2LINE فوق.
- ✅ **تسجيل D1 بقى موجود** (v1.15.0 · Worker v1.2.0) — البند القديم «مفيش
  أثر لمين طبع إيه» **اتقفل**. 🔴 **بس التسجيل في `ecommoda-constants` §7
  لسه ما اتعملش** — بند مفتوح تحت، والترتيب اللي Rule 7 بتطلبه (التسجيل
  قبل أول `writeLog`) **اتعكس** هنا.

## `?order=` — التنقل بين الصفحات

```
pack.html  →  remove.html?order=<num>&from=pack
pack.html  →  print.html?order=<num>&from=pack
```

`sku-barcode.html` بتقبل `?order=` و`?from=pack` بنفس العقد بالظبط —
**بتضيف الأوردر للدفعة وتعرضه، مابتطبعش**. لسه مفيش زرار في `pack.html`
بيروّح لها؛ إضافته قرار منفصل.

> 🔴 **الباراميتر بيفلتر ويحدّد — مايطبعش ومايحذفش.** طباعة تلقائية من رابط =
> ورق بيطلع من غير ما حد يضغط. الموظف بيراجع ويضغط بنفسه.

`from=pack` بيولّع شريط «↩ رجوع للطابور» في الصفحة اللي بتتفتح.

## `?action=` اللي الهب بيستخدمها

| الـ Worker | الـ actions |
|---|---|
| التغليف | `get_employees` · `check_employee` · `register_pin` · **`verify_employee` (+`appId`)** · **`log_logout` (+`appId`)** · `get_config` · `diag` · `get_ready_orders` · `get_order` · `complete_pack` · `get_logs*` |
| الطباعة | `get_config` · `diag` · `get_logs*` · **`bosta_lookup`** · **`bosta_awb`** · والمسارات `/orders` · `/invoice` · `/track` · `/logs` |
| الحذف | `get_config` · `diag` · `get_logs*` · وباقي مسار الحذف |
| باركود SKU | `get_config` · `diag` · **`get_order` (+`id`)** · **`search_sku`** (رجع له مستهلك في v1.15.0) · **`log_print` (POST)** · **`get_logs*`** |

> ⚠️ **الطابعة بترّوت بالـ path كمان** (`/orders` · `/invoice` · `/track` ·
> `/logs`) — شكل تاريخي متساب **عمدًا**، مش غلط يتصلّح. عشان كده `wocApi`
> بيرجّع `apiPath` جنب `apiGet`/`apiPost`.

## D1

```
tool  : warehouse_ops_center
type  : login · logout      ← بس. الهب **مابيكتبش** أي فعل تشغيلي تحت اسمه
```

الأفعال التشغيلية بتفضل تحت أسمائها التاريخية:
`order_printer`/`S1`·`S2`·**`AWB`**·**`not_found`** · `pack_checker`/`packed` ·
`order_item_remover`/`remove_item` ·
**`order_sku_barcode_printer`/`print`·`print_sku`** (جديد في v1.15.0).

> 🔴 **`print` و`print_sku` جداد ولسه ما اتسجّلوش في `ecommoda-constants` §7**
> — بند مفتوح تحت، **نفس عيلة** `AWB`/`not_found`. الأداة بتكتبهم **دلوقتي**
> (Worker v1.2.0)، والقاعدة (Rule 7) بتقول التسجيل قبل أول `writeLog`.
> `print` = ليبلات أوردر (صف لكل أوردر في الدفعة) · `print_sku` = ليبل
> لصنف **من غير أوردر** (صف لكل صنف).
> ⚠️ ولاحظ إن الصف ده **مش استثناء من «الهب واجهة بحتة»** — الكتابة بتحصل
> في `order-sku-barcode-printer-worker` تحت **اسم الأداة نفسها**، بالظبط
> زي الطباعة والتغليف والحذف.

> 🔴 **`AWB` و`not_found` جداد في v1.7.0 ولسه ما اتسجّلوش في
> `ecommoda-constants` §7** — بند مفتوح تحت. `AWB` = اتطبعت بوليصة بوسطة
> (ماشي في نفس مسار `S1` على شوبيفاي — الفرق في المستند بس)، و`not_found` =
> أوردر `Other_Regions` مالوش شحنة على بوسطة، يعني **بوابة رفع إلزامية
> اتخطّت**.

> ⚠️ `appId` **قايمة بيضاء مقفولة** في الـ Worker (`AUTH_APPS`) — جدول `logs`
> مشترك بين كل أدوات الستاك، وقبول أي نص كان معناه صفوف يتيمة بأي قيمة `tool`.

## CORS

**صفر تعديل مطلوب.** التلات Workers بيسمحوا بـ `https://ecommoda-dev.github.io`
**على مستوى الدومين**، والمسار مش جزء من الـ Origin.
و`order-sku-barcode-printer-worker` على **Option A (wildcard)**.
⚠️ **البند ده اتراجع في Worker v1.2.0** لما الأداة بقت بتكتب في D1، والقرار
إنه يفضل Option A: الكتابة الوحيدة **صف سجل append-only تحت اسم الأداة
نفسها** (صفر لمسة على شوبيفاي)، والحماية الحقيقية `WORKER_SECRET`.
⛔ لو الأداة كتبت يومًا على شوبيفاي، البند يترجع فيه لـ Option B في نفس
التسليم — التفاصيل في `Order-SKU-Barcode-Printer/CLAUDE.md` §CORS.

## النشر

```
① GitHub Pages → Settings → Pages → Deploy from a branch → main / (root)
② الصق WORKER SECRET (سر مجموعة warehouse_ops) مرة واحدة على كل جهاز
```

نسخ السر من مفتاح قديم على نفس الـ origin:

```js
localStorage.setItem('warehouse_ops_worker_secret',
                     localStorage.getItem('pack_checker_worker_secret'));
```

| | مطلوب من الريبو ده؟ |
|---|---|
| Worker جديد · `wrangler.toml` · `index.js` · تعديل CORS | ❌ كلهم |
| ربط Builds · Promote | ❌ من هنا — **بس مطلوبين في `Order-SKU-Barcode-Printer`** (تحت في المسائل المفتوحة) |

## 🔴 فحص CSS بـ parser — إلزامي، وgrep مش بديل عنه

```bash
node docs/css-check.js shared/shell.css index.html print.html pack.html remove.html journey.html stats.html sku-barcode.html
```

## 🔴 فحص متصفح فعلي — ملفان

```bash
npm i playwright jsbarcode@3.11.6 --no-save
node docs/browser-check.mjs          # print.html — ١١٦ بند
node docs/sku-barcode-check.mjs      # sku-barcode.html — ٩٢ بند
```

> ⚠️ **ملفان مش ملف واحد بقرار** — كل واحد بيشغّل Worker وهمي بشكل رد
> مختلف تمامًا (طابور طباعة وبوسطة هنا · أوردر وأصناف هناك). Worker وهمي
> واحد بيرد على أداتين معناه إن أول تعديل في رد واحدة بيكسر اختبار التانية.

> ⚠️ **الملف بقى ٩٢ بند في v1.19.0** — البنود الجديدة: **آخر أوردر اتضاف
> هو أول مجموعة** · **الدفعة بتتضاف بترتيب القايمة** (مش مقلوبة) ·
> **ترتيب الطباعة = ترتيب الشاشة** (أول ليبل من أول مجموعة) · **شريط
> التحديد فوق النتايج وثابت (`sticky`)** · وعدّ صفوف مجموعة «بدون أوردر»
> بقى بـ `bcQty_0_*` بدل المحدِّد الشقيق `~` (المجموعة بقت الأولى، والشقيق
> كان هيعدّ صفوف الأوردرات اللي بعدها).
> 🔴 **ومحدِّد مربع الاختيار بقى بيسمّي `.bc-pick-row` صراحةً** — «تحديد
> الكل» بياخد نفس كلاس `.bc-pick-cb` وبقى **أول** واحد في الـ DOM بعد ما
> الشريط طلع فوق، فـ `.first()` كانت بتعلّم «الكل» بدل أول صف **والاختبار
> يعدّي غلط**.
>
> ⚠️ **وكان ٨٧ بند في v1.17.0** — البنود اللي اتضافت وقتها: نص الانتظار «جاري
> البحث عن…» · **صف النتيجة فيه الـ SKU بس** (اسم المنتج والباركود اتشالوا
> و«مفيش Barcode» فضل) · **زرار «إضافة المحدد» متعطّل قبل التحديد وبيقول
> العدد** · **«تحديد الكل» مابيحددش المقفول** · الكمية = المتاح وعدد الطباعة
> بيتبعها · **المتاح صفر → الكمية صفر والطباعة ١** · **الـ SKU لينك بشكل
> `/products/<id>/variants/<id>` وعليه `noopener`**.
> ⚠️ **بند نص الانتظار بيتفحص بنداء `bcAcBusy` مباشرةً مش بانتظار الـ DOM** —
> الـ Worker الوهمي بيرد في نفس اللحظة، فحالة الانتظار بتظهر وتختفي في إطار
> واحد والانتظار عليها **بيعلّق الاختبار** (حصل فعلاً وقت الكتابة).
>
> ⚠️ **وكان ٧٢ بند في v1.16.0** (و٥٤ في v1.15.0) — واتضاف فيه:
> اسم التاب · **صفر كلمة «ليبل» في تاب الطباعة** · صفر `placeholder` وصفر
> `.scan-hint` · **كارت الـ SKU بعرض الصفحة وتحت الباقيين** · الاستبعاد
> بيشطب مايشيلش والضغطة التانية بترجّع · بادج التراكينج وعدّاد «N صنف»
> اتشالوا · **القايمة المنسدلة بتفتح بالكتابة والمودال اتشال** · **الـ SKU
> الكامل بمسافات بيلاقي الصنف** · **كل خلايا الجدول متوسّطة بخطوط رأسية**.
> 🔴 **والـ Worker الوهمي بقى بيطابق `search_sku` بنفس منطق `buildSkuQuery`
> بالحرف** (كلمات بادئة بـ AND) بدل `includes()`. `includes()` كان **بيخفي
> الباج**: بيرجّع نتيجة على `SD1 / Light grey / 45` بينما شوبيفاي الحقيقي
> بيرجّع صفر — يعني الاختبار كان بيعدّي والأداة مكسورة.
> ⚠️ وأوردر تالت (`#53770`) اتضاف للتراكينج بس: البند ⑦ كان معتمد على إن
> البند ⑥ حذف مجموعة `#53769` قبله، والاستبعاد بقى شطب مش حذف.
> ⚠️ والـ Worker الوهمي بيرد على `search_sku` و`log_print` و`get_logs*`
> و`get_employees` كمان، وبيقرا **جسم الـ POST** فعلاً.
>
> 🔴 **`docs/sku-barcode-check.mjs` اتكتب أصلاً عشان بند واحد:** «٥ ليبل
> بيطلعوا ٣٧ صفحة». البند بيقيس **ارتفاع `document.body` وقت الطباعة** مقسومًا على
> ارتفاع الورقة (١ إنش = 96px) ويقارنه بعدد الليبلات — لو باقي الصفحة لسه
> واخد مكانه في التخطيط، الرقم بيطلع أكبر بمراحل. **وده اللي كشف السبب
> التاني** (`min-height:100vh`) بعد ما الأول (`visibility:hidden`) اتصلح؛
> مراجعة الكود كانت هتقف عند الأول وتقول خلاص.

> ⚠️ **JsBarcode بتتقدّم من `node_modules` مش من الـ CDN** في الاختبار —
> من غير كده الاختبار معتمد على شبكة خارجية، وحارس `bcLibReady()` بيقفل
> زرار الطباعة فبنود الليبل والطباعة **بتفشل لسبب مالوش علاقة بالكود**.
> و`PW_CHROMIUM` بيسمح بتمرير مسار كروميوم مثبّت مسبقًا لو Playwright
> ما نزّلش نسخته.

> ⚠️ **الملف بقى ١١٦ بند في v1.18.0** — البنود الجديدة: **سؤال البوابة
> الواحد** (بالعدد وبالنص مع بعض) · **غياب `bostaUpdated`** من الإقرار ·
> **ترتيب تاب البوليصة** (`preopen → close → preopen → nav` — الترتيب هو
> البند مش وجود الخطوات) · **شارة §LAST-DOC** في الجدول وفي البوابة
> وبتحذيرها المشروط (أوردر بنفس النوع **مالوش** تحذير) · **اسم المهمة جوّه
> ملف الـ PDF** مقروء من البلوب اللي التاب اتوجّه له فعلاً.
> 🔴 **والـ PDF الوهمي بقى ملف صالح بجدول xref** بدل نص شبيه —
> `pdfWithTitle` بترجّع أي ملف مش مفهوم **زي ما هو**، يعني ملف وهمي مكسور
> كان هيخلّي بند العنوان **يعدّي وهو مش بيتنفّذ أصلاً** (نفس درس
> `includes()` في فحص الباركود).

بيشغّل `print.html` في كروميوم حقيقي على **Worker وهمي** ويفحص ١١٦ بند:
مبدّل القناة والعدّادات · الصفوف المقفولة بلا زون · إخفاء S2 بوسطة بالعدد ·
مسح التحديد عند تبديل القناة · بوابة السؤالين (**بمربع واحد متعلّم لازم
تفضل متعطّلة**) · `type`/`doc` في `/track` · `bostaUpdated` في الإقرار ·
**وانحدار مسار الفاتورة** (قاهرة+جيزة وشو روم لسه بينادوا `/invoice`) ·
**حارس تاج الرفع** (`Bosta_Uploaded_S1`) · **§JUST-PRINTED** (الأوردر المطبوع
بيختفي رغم إن الـ Worker الوهمي لسه بيرجّعه — محاكاة فهرس شوبيفاي بالحرف) ·
**§PRINT-BAR** (الفلاتر مطوية وبتفتح بضغطة · البادجات بتساوي الجدول ·
الزراير بتتعطّل ما بتختفيش) · **§AWB-TAB** (التاب بيتحجز عند الضغطة
`preopen` وبيتوجّه لملف بلوب `nav` — **بالترتيب ده**؛ والشريط الاحتياطي
ما بيظهرش لما الفتح ينجح) · **§COUNT-BOX** (الرقم الكبير وبادج «تحديث»
== صفوف الجدول · لوحة «آخر تحديث» اتملّت بعد الجلب · ختم الهيدر القديم
اتشال) · **§COLUMNS** (عمود «تاريخ الأوردر» موجود وعمودا «التاريخ»/«الوقت»
اتشالوا من الطابور **بس** — تاب السجل لسه عنده الاتنين · «نوع الأوردر» نص
مش بادج · الصف تمن خلايا · بادج العمر في كل صف).

**ليه:** عطلان في نموذج بوسطة الأولي (مودال الإعدادات بيفتح فوق شاشة الدخول ·
الفوكس بيتسحب جوّه الـ PDF) **ما اتمسكوش بمراجعة كود** — اتمسكوا بتشغيل
الصفحة في متصفح. ونفس الحكاية مع `direction:ltr` في `stats.html`.

> ⚠️ **فخّان في كتابة الاختبار نفسه:** `frame.evaluate()` على frame فيه PDF
> **بتعلّق للأبد** في هيدلس (استخدم `exposeFunction`)، و`browser.close()`
> بتعلّق برضه لو فيه frame PDF مفتوح (سباق بمهلة).

**السبب:** v1.0.0 اتنشرت وشكلها مكسور تمامًا (خطوط serif · كروت شفافة ·
مودالات بتتعرض في مجرى الصفحة) **من غير أي خطأ في الكونسول**. فحص Step 9
كله عدّى، لأنه grep — بيعدّ نصوص، مابيقراش CSS.

**اللي البارسر مسكه واللي الـ grep فاته:**

| المشكلة | الأثر |
|---|---|
| **تعليق CSS متداخل** في هيدر `shell.css` — الهيدر كان فيه مثال `<style> /* … */ </style>` | تعليقات CSS **مابتتداخلش**: أول `*/` قفل الهيدر بدري، وباقي النص اتقرا CSS، والبارسر بلع **كتلة الـ ٥١ توكن كلها** جوّه selector غلط. كل `var(--x)` بقت فاضية |
| **`var(` مش مقفولة** في `remove.html` (`var(--green-dark3d;`) — استبدال ناقص وقت إصلاح الـ hex | **كل** الـ `<style>` بتاع الصفحة فشل يتقرا |
| **`--accent-muted` مستخدمة ومش معرّفة** — كانت في `:root` بتاع الطابعة واتشالت مع التوكنز | `.btn-primary:disabled` بلا خلفية |

> 🔴 **القاعدة:** ممنوع أي علامة تعليق جوّه تعليق CSS. لو محتاج تكتب مثال
> فيه `<style>`، اكتبه بـ `...` بدل علامات التعليق.

> ⚠️ **الفشل ده صامت بالكامل** — الصفحة بتفتح، والـ JS بيشتغل، والكونسول
> نضيف. الحاجة الوحيدة اللي بتمسكه parser. الفحص ده بيتنفّذ **مع** Step 9
> مش بدله.

## 🔴 `checks` في `diag` ليها شكلين مختلفين

| الـ Worker | الشكل |
|---|---|
| الطباعة · التغليف | **مصفوفة** `[{ ok, label\|name, detail }]` |
| **باركود SKU** | **مصفوفة** `[{ ok, label, detail }]` — الشكل المعتمد للجديد |
| حذف منتج | **كائن عادي** `{ d1: 'ok', oauth: 'FAILED: …' }` |

`diagRows()` في الـ shell بتفهم الاتنين. v1.0.0 كانت بتعمل `for…of` على
الاتنين، فالكائن كان بيرمي «object is not iterable» و**بيوقّف الفحص كله**
عند أول Worker بالشكل ده — الموظف مايشوفش نتيجة الأدوات اللي بعده.

> ⚠️ في شكل الكائن **مفيش `ok` صريحة**، فالحكم من القيمة والاسم:
> `'ok'` ✅ · بتبدأ بـ `FAILED` ❌ · الاسم منتهي بـ `Error`/`Warning` ⚠️ ·
> غير كده **معلومة** ℹ️ (زي `accessScopes`) — مش نجاح ولا فشل.

## فحص Step 9

الأمر اللي اتنفّذ فعليًا على الست صفحات موجود في `docs/` بتاع
`Orders-Packing-Checker` §⑫. النتيجة وقت آخر تعديل: **كل البنود عدّت**، مع
انحراف واحد **موثّق ومقصود**: z-index الهيدر الثابت في `print.html`
(300/310) أعلى من «محتوى ≤ 200` — السبب فوق في فخاخ الطابعة.

## مسائل مفتوحة

- ✅ **`BOSTA_API_KEY` اتضاف على `orders-packing-checker-worker`** (أحمد،
  05-09-2026) — مدخل بوسطة في `pack.html` بقى شغّال. الفحص النهائي لسه
  مطلوب: 🩺 فحص النظام لازم يوري بند مفتاح بوسطة ✅، وأول سكان بتراكينج
  حقيقي لازم يعرض نفس رقم التتبع اللي على الملصق.
- 🔴 **Promote لـ `order-item-remover-worker` v1.4.0** — من غيره حارس §PREFLIGHT
  مش موجود والحذف بيفضل بلا حماية (البند بيتشال والحالة/السبب مايتكتبوش)، و
  `remover.min = 1.4.0` هيولّع «⚠️ Worker حذف منتج نسخة قديمة». **وقبل الـ
  Promote القيمة الغلط لسه منشورة** — يعني كل حذف لسه بيفشل في كتابة السبب.
- 🔴 **Promote لـ `order-printer-worker` v2.4.0** — من غيره `POST /lookup`
  مابيردّش، و**زرار «إعادة 🖨» في طابور التغليف مايشتغلش** (الكارت بيقول بالنص
  إن النسخة أقدم). وبيشيل كمان بند v2.3.0 اللي تحت.
- ✅ **ربط `Order-SKU-Barcode-Printer` بـ Workers Builds اتعمل** (أحمد،
  08-09-2026) — البند ده كان مفتوح من أول يوم. أي push على `main` دلوقتي
  بينشر `index.js` من الريبو، ونشر v1.3.0 بيمشي بالمسار ده.
- 🔴 **باقي على `order-sku-barcode-printer-worker` تلات بنود يدوية:**
  ① **`WORKER_SECRET` = قيمة مجموعة `warehouse_ops` → Promote** — من غيره
  الصفحة بترجّع `401` على كل نداء.
  ② **الـ build لازم ياخد `[[d1_databases]]`** (binding `DB`) — الـ binding
  بيتقرا من `wrangler.toml`، ومن غيره `log_print` بيرد 500 برسالة صريحة
  («binding قاعدة البيانات ناقص»).
  ③ **صلاحية `read_inventory` في الـ Custom App** — 🟡 **مش حاجزة**:
  الـ Worker بيرجع لاستعلام لين من غيرها والبحث بيفضل شغّال، وخانة «الكمية»
  بتقول `—`.
  ⚠️ `?action=diag` هو اللي بيثبت التلاتة: **البصمة** لـ① · **`D1 (DB)`**
  لـ② (بيعدّ صفوف الأداة فعلاً، فـ binding على قاعدة غلط بيبان ❌ مش ✅) ·
  **`صلاحية read_inventory`** لـ③. الإجراء الكامل في `CLAUDE.md` بتاع الريبو ده.
- 🔴 **مين بيكتب تاج `Bosta_Uploaded_S1`؟ — بند مفتوح ولازم يتقفل.** طابور
  بوسطة بقى مشروط بالتاج ده من v1.11.0، والقياس يوم النشر: **صفر من ٦٠**
  أوردر في الطابور عليه التاج، والتاج موجود على **أوردرين في المتجر كله**
  (`#53849` · `#53834`) — الاتنين اتطبعوا بالفعل. يعني الطابور هيبان
  **فاضي** لحد ما أداة الرفع على بوسطة تبدأ تكتب التاج على كل أوردر بترفعه.
  المطلوب: تحديد الأداة/الفلو اللي بترفع على بوسطة والتأكد إنها بتكتب التاج
  بنفس الحروف بالظبط.
- 🟡 **Promote لـ `order-printer-worker` v2.6.0 (`statusAfter`)** — **مش
  حاجز**: من غيره §JUST-PRINTED بيرجع لـ `status === 'success'` وبيشتغل
  عادي (التدهور آمن، و`printer.min` ما اترفعش عن قصد). بيتعمل مع Promote
  v2.5.0 المطلوب أصلاً تحت.
- 🔴 **`BOSTA_API_KEY` + Promote لـ `order-printer-worker` v2.5.0** — من غيرهم
  **قناة بوسطة مش شغّالة أصلاً** (`bosta_lookup` بترجّع 404)، وحارس النسخة
  بيقول «Worker الطباعة نسخة قديمة». الإجراء الكامل في
  `Order-Printer/CLAUDE.md` §«مطلوب بعد v2.5.0».
- 🔴 **تسجيل `AWB` و`not_found` في `ecommoda-constants` §7** تحت
  `order_printer`، و`bosta_awb_print` كقيمة **متقاعدة** (صفوف تجربة
  05-09-2026 بتتساب زي ما هي بقرار). الأداة بتكتب القيم دي **دلوقتي**،
  والتسجيل شرط قبل أول `writeLog`.
- 🔴 **تسجيل `order_sku_barcode_printer` في `ecommoda-constants` §7** بقيم
  `print` · `print_sku` — **صف `tool` جديد بالكامل**، مش قيم `type` زيادة
  على صف قايم. الأداة بتكتبهم **دلوقتي** (Worker v1.2.0)، والترتيب اللي
  Rule 7 بتطلبه (التسجيل **قبل** أول `writeLog`) **اتعكس** — نفس اللي حصل
  مع `AWB`/`not_found` فوق. **مطلوب تحديث مهارة قبل أول تشغيل حي.**
- 🟡 **قياس `zoneExcluded` قبل النشر** — نداء `/orders` من غير `allZones`
  بيقول كام أوردر في الطابور زونه فاضي أو غير معروف. لو الرقم كبير، الصفوف
  دي محتاجة قسم منفصل مش سطور وسط الجدول.
- 🔴 **Promote لـ Worker التغليف v2.5.0** — من غيره الدخول بيتسجّل
  `pack_checker` **في صمت** والهب هيطلّع «⚠️ Worker التغليف نسخة قديمة».
- 🟡 **أوردر متغلّف بيفضل عالق في طابور «جاهز للتغليف» لو اتطبعت فاتورته تاني
  بعد التغليف** — موروث. التفاصيل في
  `Orders-Packing-Checker/docs/known-issue-reprint-stuck-in-queue.md`.
- 🟡 **الأدوات القديمة التلاتة لسه شغّالة بالتوازي** — بقرار، كنقطة رجوع.
  تحويلها لصفحات تحويل قرار منفصل بعد نجاح التجربة.
- 🟡 **`classifyS2Subtype` و§BOSTA في ريبوهين** — موروث، في الـ Workers مش في
  الواجهة. الهب مابيحلهوش.
- 🟢 **مفيش session token موقّع** — الهوية لسه من العميل، زي الأدوات المستقلة
  بالظبط.
- 🟢 **مفيش `metafields_change` في رحلة الأوردر** — محتاج مصدر رابع.
- 🟢 **مفيش توحيد لـ CSS الجداول بين الصفحات** — قاعدة الأسبقية بتخلّي كل
  صفحة بجدولها. توحيدها تمريرة منفصلة لو التكرار كبر.
- 🟡 **خمس صفحات لسه مبصومة بإصدارات مهارات قديمة** (`print` · `pack` ·
  `remove` · `journey` · `stats` → html-builder `v6.3.0`). تعديل v1.9.0 على
  `print.html` كان **كبير** (قناة بوسطة)، بس البصمة **ما اترفعتش** لأن
  تمريرة المراجعة الكاملة على html-builder v6.6.0 ما اتعملتش — والبصمة
  **ادعاء إن الملف اتراجع على الإصدار ده**، فرفعها من غير مراجعة فعلية تزوير. المطلوب تمريرة مراجعة على كل صفحة
  مقابل html-builder v6.6.0 (بنود ٣٠ · ٣٥–٣٨ · ٤٠–٤٢ اتضافوا بعد بنائهم)،
  والبصمة تترفع ساعتها.
- 🟡 **`print.html` و`pack.html` بشكل سجل تغييرات مختلف** — بيستخدموا
  `.cl-entry`/`.cl-ver`/`#clLatestBadge` بدل `.cl-version-block`/
  `.cl-ver-badge`/`#clLatestVerBadge`. النتيجة إن `renderVersionUI()`
  **مابتلمسش** بادج النسخة عندهم، فالرقم مكتوب بإيد في الملفين — مخالفة
  Standards #24 (مصدر واحد للنسخة). التوحيد تمريرة منفصلة؛ لحد ساعتها **أي
  رفع نسخة لازم يتعمل يدوي في الملفين**.
- 🟢 **مفيش زرار من `pack.html` لـ `sku-barcode.html`** — الصفحة بتقبل
  `?order=`&`from=pack` جاهزة، بس إضافة الزرار قرار منفصل.
- ✅ **`search_sku` رجع له مستهلك (v1.15.0)** — البند اللي كان مفتوح
  («يتشال ولا يفضل؟») **اتقفل بالإبقاء**: مربع «طباعة بالـ SKU» بينادي
  المسار ده. **الشيل بقى تغيير كاسر، مش تنضيف.**
- 🟡 **`bindScannerInput` و`.entry-*` مكرّرين حرفيًا بين `pack.html` و
  `sku-barcode.html`** — تكرار **مقصود** (كل صفحة بـ CSS و JS بتوعها،
  قاعدة الأسبقية)، بس الخطر حقيقي: تعديل في واحدة من غير التانية = سلوك
  سكانر مختلف بين صفحتين الموظف بيتنقّل بينهم بنفس السكانة. نقلهم للـ shell
  تمريرة منفصلة — و`bindScannerInput` **مش** في القايمة السودا فهو مرشّح
  مشروع، بخلاف `lookupOrder` اللي منطقه مختلف فعلاً بين الصفحات.

## 📄 خطة أداة «التنبيهات» — `docs/ALERTS-TOOL-PLAN.md`

صفحة جديدة مقترحة في الهب بتعرض **الحالات الشاذة اللي محتاجة تدخّل** (اتعدّل بعد
الطباعة/التغليف · اتلغى بعد التغليف · أوردر بلا `zone` مختفي من كل الطوابير …).

الملف فيه: الاسم و`tool` المقترح · تحليل من فين البيانات (**أغلب القواعد بتتحسب
من D1 لوحده**) · كتالوج القواعد بالخطورة وشرط الإقفال التلقائي · دورة حياة
التنبيه · شكل الصفحة · خطة على ٤ مراحل · المخاطر · **٨ قرارات مستنية أحمد**.

> 🔴 الملف ده **خطة مش تنفيذ** — بيتقرا في جلسة تنفيذ مستقلة، وقرارات §12
> بتتاخد قبل أول سطر كود. وأول بند فيها إن الأداة دي **ليها Worker** — يعني
> استثناء صريح من «الهب واجهة بحتة»، لازم يتكتب هنا وقت التنفيذ مش يعدّي بالصمت.

## 📄 خطة صفحة «إحصائيات اليوم» — `docs/STATS-PAGE-PLAN.md`

صفحة جديدة مقترحة في الهب بتعرض **إيه اللي حصل للمخزن النهاردة** في شاشة واحدة:
أرقام كبيرة (طباعة · تغليف · قطع · حذف) · جدول الموظفين · خط زمني بالساعة ·
مؤشرات جودة (فواتير لاغية اتقطعت · إعادة طباعة · تغليف بتحذير · حذف فشل).

**قراءة D1 بحتة عبر `get_logs` في التلات Workers** — صفر تعديل Worker، صفر
Promote، صفر ترفيع لأي `min`، ومفيش قيمة `tool` جديدة (الصفحة مابتكتبش صف واحد).

الملف فيه: عقد البيانات وتصنيف الحمولة (**أحداث** مش حالة) · تكلفة التصفيح
الحقيقية · **فخ يوم القاهرة** · كتالوج الأرقام · **الأرقام الممنوعة وليه** ·
سلوك الفشل · خطة على ٣ مراحل · المخاطر · **٥ قرارات مستنية أحمد**.

> 🔴 **أخطر بندين في الملف، والاتنين بينتجوا رقم غلط شكله سليم:**
> **①** `dateFrom`/`dateTo` في التلات Workers بيفلتروا بـ **يوم UTC**، والعرض
> بتوقيت القاهرة — يعني «النهاردة» بتفقد آخر ٣ ساعات من الوردية من غير أي خطأ.
> الحل بلا تعديل Worker: اطلب **يومين UTC** وفلتر في الواجهة بـ `cairoDayStr`.
> **②** `extra.result` ليها **تلات أشكال**: كائن في الطباعة، ونص في التغليف
> والحذف — فقارئ موحّد بيخلّي مؤشر تحذير الطباعة **صفر للأبد** في صمت.

> ⚠️ **الحضور (`login`/`logout`) خارج النطاق بقرار** (أحمد 06-09-2026):
> `buildLogFilterSQL` بيستبعدهم **في SQL** في التلات Workers، فإدخالهم كان
> هيتطلّب تعديل التلاتة + ٣ Promotes. النتيجة اللي لازم تتقال في الشاشة نفسها:
> المربع اسمه **«موظفين عملوا عمليات»** مش «الحضور».

> ✅ **الخطة دي اتنفّذت في v1.5.0** (06-09-2026) — القرارات الخمسة الباقية
> اتاخدت والملف اتحدّث بيها. بيفضل مرجع القرارات والأرقام الممنوعة؛ الفخاخ
> التنفيذية اتنقلت لقسم `stats.html` فوق.

## تحديث المهارات — مطلوب

**`docs/SKILLS-UPDATE-PACK.md`** فيه كل اللي لازم يتحدّث في المهارات بعد بناء
الهب: **٨ تعديلات في ٤ مهارات**، كل واحد بنصّه الجاهز وتصنيف أثره ورقم النسخة
المقترح. **الأربعة الإلزامية:** بند ٤٢ (فحص CSS بـ parser — **ابدأ بيه**) ·
تعديل Step 9 (٤ فحوص بترجّع نتيجة كاذبة) · بند ٤٠ (استثناء الهب) · §6 في
`ecommoda-constants` (مجموعة `warehouse_ops` بقى فيها مستهلك رابع مش Worker).

> 🔴 **بندان مصدرهم فشل حيّ بعد النشر** (٤٢ و٨) — يعني كل مراجعة وكل فحص
> قبلهم **عدّوا عليهم**. دول أغلى بنود في الملف.

> ⚠️ **الملف ده بيتقرا في محادثة تحديث مهارات مخصّصة** — مش في محادثة بناء أدوات.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-html-builder | **v6.6.0** |
| ecommoda-worker-builder | **v2.1.0** |
| ecommoda-constants | **v1.10.0** |
| ecommoda-order-lifecycle | v1.2.0 |
| shopify-graphql-helper | **v1.1.0** |
| ecommoda-tool-migration-playbook | §13 (Promote) |

> ⚠️ **البصمة دي بتوصف `shared/shell.js` و`shared/shell.css` و`index.html`
> و`sku-barcode.html` بس** — دول اللي اتراجعوا فعليًا على الإصدارات دي في
> تعديل v1.6.0 (و`sku-barcode.html` اتراجعت تاني في v1.15.0 و v1.16.0 و v1.17.0،
> والجدول فيها اتحوّل للمعيار الموحّد §Data Tables في التمريرة دي). الخمس صفحات
> التانية لسه على بصمتها القديمة (بند في المسائل المفتوحة فوق) —
> **البصمة ادعاء عن الملف، مش عن الريبو**.

آخر مطابقة: 09-09-2026 · الهب `v1.19.0`
🔴 معلّقة: تسجيل `order_sku_barcode_printer` في `ecommoda-constants` §7 ·
مراجعة `print`/`pack`/`remove`/`journey`/`stats` على html-builder v6.6.0

---

آخر تحديث: 09-09-2026 — v1.19.0

</div>
